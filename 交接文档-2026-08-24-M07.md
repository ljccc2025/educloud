# 交接文档-2026-08-24-M07（educloud-order 订单中心与交易履约）

> 面向：下一位接手 EduCloud 微服务开发的工程师。  
> 模块：M07 订单与交易中心（`educloud-order`）及前端三端交易收银台——**已全部完成并验收**，本文是交接与继承指引。  
> 生成时间：2026-08-24  

---

## 0. 一句话状态

M07 订单中心**全部功能完成、通用门禁全部达标、三端联调打通、全链路 E2E 自动化测试 100% 通过、Playwright 自动化截图留存**；  
涵盖购物车管理、10 分钟 Redis 防重 Token、OpenFeign 实时查价与快照固化、交易提单状态机、RabbitMQ 15 分钟 TTL + DLX 死信延时关单、模拟支付与 `OrderPaidEvent` 广播自动开课、内部微服务 RPC 快照查询与管理端全量订单审计；  
所有代码已全量提交并推送到 GitHub `main` 分支（Commit `bfba937`），虚拟机（192.168.100.136）全套 **8 个微服务均处于 `UP` 状态**；下一步为 **M08（educloud-payment 支付中心）**。

---

## 1. 代码与文档位置

| 项目 | 位置 |
|---|---|
| 唯一权威工作区 | `D:\microservice\.worktrees\educloud-backend-foundation`（**main 分支**，git 仓库） |
| 远端仓库 | GitHub `https://github.com/ljccc2025/educloud.git`（0 ahead / 0 behind） |
| M07 设计规格 | `docs/superpowers/specs/2026-08-24-educloud-order-design.md` |
| M07 交付计划与清单 | `docs/superpowers/specs/2026-08-24-m07-delivery-and-handover.md` |
| 历史交接文档 | `交接文档-2026-08-22.md`（M01-M03）、`交接文档-2026-08-23-M04.md`（文件）、`交接文档-2026-08-24-M05.md`（课程）、`交接文档-2026-08-24-M06.md`（内容） |
| 本交接文档 | `交接文档-2026-08-24-M07.md` |

**工作区纪律**：所有开发只在 worktree 进行；改完提交并推送至 GitHub `main` 分支；  
VM 上对应目录 `/root/educloud/.worktrees/educloud-backend-foundation` 是**文件副本（非 git 仓库）**，本地改动通过 `python scratch/sync_files_to_vm.py` 脚本增量同步与部署。

---

## 2. 模块范围与交付物

M07 = 交易订单服务全链路：**购物车 CRUD / 10 分钟 Redis 防重 Token / 远程 Feign 查价与快照固化 / 交易订单生成与状态机 / CAS 原子主动取消 / RabbitMQ 15 分钟延时关单 / 模拟支付与 OrderPaid 事件广播 / Course 端监听自动开课 / 内部微服务 RPC 快照 / 管理端全量流水 / 学生端收银台与我的订单 / 管理端订单管理列表与详情弹窗**。

### 2.1 核心业务流程与领域架构

1. **购物车管理（Cart CRUD）**：
   - 支持单个课程重复加购幂等处理；
   - 支持选中状态切换（单个勾选/全选/反选）；
   - 支持移除商品条目与一键清空购物车；
   - 购物车汇总动态计算已选数量与已选应付总额。
2. **下单防重 Token 机制（Idempotency Token）**：
   - 客户端提单前通过 `GET /api/v1/orders/idempotency-token` 申请 32 位 UUID 令牌；
   - 服务端存入 Redis，Key 格式 `educloud:order:idempotency:{token}`，TTL 10 分钟；
   - 提单时通过 Redis Lua 脚本执行原子“比较并删除”，防范网络重试与高并发连击重复扣款。
3. **OpenFeign 远程查价与课程快照固化**：
   - 提单时调用 `educloud-course` 的 `CourseClient`（`GET /api/v1/courses/{id}`）获取实时上架状态与价格；
   - 拦截未上架或已下架课程（抛出 `COURSE_NOT_ON_SALE`）；
   - 固化 `courseTitleSnapshot`、`coverFileIdSnapshot`、`unitPriceSnapshot` 到 `trade_order_item`，不受后续课程改价/改名影响。
4. **交易订单生成与状态机（State Machine）**：
   - 状态枚举：`PENDING_PAYMENT`（待支付）➔ `PAID`（已支付）/ `CANCELLED`（已取消/超时关闭）/ `REFUNDED`（已退款）；
   - 履约状态：`UNFULFILLED`（未履约）➔ `FULFILLED`（已履约，开课完成）；
   - 支持学员主动取消待支付订单（CAS 校验更新 `status = CANCELLED WHERE status = PENDING_PAYMENT`）；
   - 订单生成时自动设置 15 分钟失效时间 `expires_at`。
5. **RabbitMQ 15 分钟 TTL + DLX 死信延时关单**：
   - 声明延时交换机 `educloud.order.delay.exchange` 与延时队列 `educloud.order.delay.queue`（`x-message-ttl: 900000`）；
   - 声明死信交换机 `educloud.order.dlx.exchange` 与死信关单队列 `educloud.order.close.queue`；
   - 订单创建后发送延迟消息，15 分钟超时未支付自动路由至死信队列，`OrderDelayConsumer` 执行 CAS 幂等关单。
6. **模拟支付与自动开课联动（`OrderPaidEvent`）**：
   - 学生端发起 `POST /api/v1/orders/{id}/mock-pay`；
   - CAS 更新订单状态为 `PAID`、履约状态为 `FULFILLED`，并广播 `OrderPaidEvent` 到 TopicExchange `educloud.order.exchange`（RoutingKey: `order.paid`）；
   - `educloud-course` 微服务消费该事件，调用 `EnrollmentService` 自动为该学员创建选课记录，实现“支付成功即自动开通课程学习权限”。
7. **内部微服务契约与管理端审计**：
   - 提供内部 RPC 接口：`GET /internal/v1/orders/{id}/payable-snapshot` 与 `GET /internal/v1/orders/{id}/fulfillment-snapshot`；
   - 管理端 `GET /api/v1/admin/orders` 支持全量订单流水多条件过滤（状态、订单号）与明细弹窗查看。

### 2.2 关键端点清单（经网关 /api/v1）

| 端 | 端点路径 | 请求方式 | 鉴权/权限 | 说明 |
|---|---|---|---|---|
| 学生 | `/cart` | GET | Bearer Token | 查询个人购物车列表及汇总金额 |
| 学生 | `/cart/items` | POST | Bearer Token | 添加课程到购物车 |
| 学生 | `/cart/items/{id}/selection` | PUT | Bearer Token | 切换商品勾选状态 |
| 学生 | `/cart/items/{id}` | DELETE | Bearer Token | 移除购物车指定商品 |
| 学生 | `/cart/clear` | DELETE | Bearer Token | 清空个人购物车 |
| 学生 | `/orders/idempotency-token` | GET | Bearer Token | 获取下单防重 Token（10 分钟有效） |
| 学生 | `/orders` | POST | Bearer Token | 提交结算生成待支付订单（支持单买/购物车） |
| 学生 | `/orders` | GET | Bearer Token | 分页查询个人历史订单列表（支持状态过滤） |
| 学生 | `/orders/{id}` | GET | Bearer Token | 查询个人订单详情与商品快照 |
| 学生 | `/orders/{id}/cancel` | POST | Bearer Token | 主动取消待支付订单（CAS 校验） |
| 学生 | `/orders/{id}/mock-pay` | POST | Bearer Token | 模拟支付，触发自动开课事件 |
| 内部 | `/internal/v1/orders/{id}/payable-snapshot` | GET | Service Secret | 内部 RPC：支付网关查验应付金额与状态 |
| 内部 | `/internal/v1/orders/{id}/fulfillment-snapshot` | GET | Service Secret | 内部 RPC：履约中心获取商品快照 |
| 管理 | `/admin/orders` | GET | `order:admin` | 管理端全量订单流水列表与筛选 |
| 管理 | `/admin/orders/{id}` | GET | `order:admin` | 管理端查看订单完整快照与履约状态 |

### 2.3 前端三端对接与交互

1. **学生端（`student-portal` : 5173）**：
   - 课程详情页（`CourseDetail.tsx`）集成“立即购买”直达结算收银台；
   - 结算收银台（`Checkout.tsx`）支持单课直接购买与购物车结算，自动获取防重 Token，集成模拟支付与“支付成功”结果面板，支持“开始学习”直达课程播放页；
   - 我的订单页（`Orders.tsx`）展示历史订单列表、15 分钟失效倒计时剩余分秒、待支付状态下支持“去支付”与“取消订单”。
2. **管理端（`admin-portal` : 5175）**：
   - 订单管理页面（`OrderManage.tsx`）接入真实后端 `/api/v1/admin/orders` 分页接口；
   - 支持按全部/待支付/已完成/已取消过滤及订单号精确检索；
   - 状态徽标全局注入 `whitespace-nowrap shrink-0` 修复不折行；
   - 点击“详情”弹出订单明细弹窗，展示商品快照标题、单价、条目小计与履约状态。

### 2.4 数据库迁移脚本

- `deploy/sql/order/V000__technical_tables.sql`：MyBatis-Plus 分布式雪花序列与 Outbox 事件表；
- `deploy/sql/order/V001__init_order_schema.sql`：`cart_item`, `trade_order`, `trade_order_item`, `refund_request`, `refund_request_item` 业务表；
- `deploy/sql/order/V002__order_seed_data.sql`：预置购物车与订单测试种子数据；
- `deploy/sql/user/V007__order_permissions.sql`：为学生、教师、财务与管理员角色注入 `order:create/view/cancel/refund/admin` 权限码（121-125）。

---

## 3. 技术栈与架构契约

- **技术栈**：Java 17 / Spring Boot 3.2.5 / Spring Cloud 2023.0.1 / Spring Cloud LoadBalancer / MyBatis-Plus 3.5.12 / MySQL 8.0.36 / Redis / RabbitMQ / Nacos / React 18 / Tailwind CSS。
- **微服务端口规划（双端口标准模式）**：
  - `educloud-gateway`：`8080`（业务） / `8081`（监控）
  - `educloud-user`：`8082`（业务） / `8083`（监控）
  - `educloud-content`：`8085`（业务） / `8086`（监控）
  - `educloud-file`：`8087`（业务） / `8088`（监控）
  - `educloud-course`：`8089`（业务） / `8090`（监控）
  - `educloud-order` (M07)：`8091`（业务） / `8092`（监控）
  - 前端三端：`5173`（学生） / `5174`（教师） / `5175`（管理）
- **核心组件配置规范**：
  - `IdentifierConfig.java`：基于 `IdWorker::getId` 注入 64 位雪花算法主键生成器；
  - `JwtDecoderConfiguration.java` 与 `JwksLoader.java`：通过 `ORDER_JWKS_LOCATION`（`/tmp/educloud-live/jwks.json`）动态验签 RSA JWT Token；
  - `HealthIndicatorConfig.java`：聚合 MySQL、Redis、RabbitMQ、Nacos 的深度健康检查。

---

## 4. 环境信息与运维指南

| 项 | 说明 / 值 |
|---|---|
| 虚拟机 IP | `192.168.100.136`（Rocky Linux 8.9，root 用户 / 密码 `1`） |
| 代码文件路径 | `/root/educloud/.worktrees/educloud-backend-foundation` |
| 一键启动脚本 | `bash deploy/scripts/start-dev.sh`（位于 VM 仓库根目录下，幂等执行） |
| 代码同步脚本 | 本地执行 `python scratch/sync_files_to_vm.py` 自动增量上传修改并编译 |
| 就绪探测脚本 | 本地执行 `python scratch/fix_crlf_and_start.py` 自动重启与就绪校验 |
| 预置账号（学生） | `fe_demo_10` / `FeDemo@2026`（Snowflake ID: `2091648316809035778`） |
| 预置账号（教师） | `demo_teacher@educloud.cn` / `EduCloud@2026`（ID: `9000000000000000001`） |
| 预置账号（管理） | `demo_admin` / `EduCloud@2026`（ID: `9000000000000000002`） |

---

## 5. 验证与验收记录

1. **后端全量编译与单元测试**：
   - 全套 8 个微服务模块（`common`, `gateway`, `user`, `file`, `course`, `content`, `order`）本地与虚拟机执行 `mvn clean test-compile`，**BUILD SUCCESS，0 错误**。
2. **前端构建验证**：
   - `student-portal`、`teacher-portal`、`admin-portal` 三端执行 `tsc && vite build` 均 **0 报错，顺利输出 dist**。
3. **E2E 全链路集成自动化测试（`scratch/test_order_e2e.py`）**：
   - 步骤 1：学员登录（`fe_demo_10`）➔ 获取 AccessToken；
   - 步骤 2：动态发现已上架课程（`9000000000000000113`，¥199.00）并添加至购物车；
   - 步骤 3：获取防重 Token（32 位 UUID）；
   - 步骤 4：提单生成 `PENDING_PAYMENT` 订单（`ORD2091895618182258690`）；
   - 步骤 5：发起模拟支付 ➔ 订单状态跃迁为 `PAID`；
   - 步骤 6：RabbitMQ 异步广播事件 ➔ 课程中心自动开课（`enrolled: True`）；
   - 步骤 7：管理员登录（`demo_admin`）➔ 分页查询全量流水及商品快照；
   - **结果：7 大阶段 100% 全部断言通过！**
4. **Playwright 浏览器 UI 自动化实测与截图留存（`scratch/screenshots/`）**：
   - `01_student_login_page.png`：学生端登录页面；
   - `02_student_logged_in.png`：学生登录后首页；
   - `03_course_detail_page.png`：课程详情与“立即购买”按钮；
   - `04_checkout_page.png`：收银台结算与支付方式选择；
   - `05_payment_result.png`：模拟支付成功结果；
   - `06_student_orders_page.png`：我的订单列表与状态展示；
   - `07_admin_login_page.png`：管理端登录界面；
   - `08_admin_logged_in.png`：管理端仪表盘；
   - `09_admin_order_manage_table.png`：订单管理表格与单行状态徽标；
   - `10_admin_order_detail_modal.png`：订单明细快照弹窗。

---

## 6. 近期 Git 提交记录（供追溯）

```text
bfba937 fix(student): 增强结算页路由参数解析与构建
0188c4a fix(student): 增强 getMyOrders 数据解包容错与函数重载签名
8cd4d5d fix(order): 切换数据库连接用户为 order_app
87c5e84 fix(order): 修正 JDBC URL characterEncoding 为 utf8
9afd48f fix(order): 完善 application.yml 中 Nacos/MySQL/Redis/RabbitMQ 配置
693ace1 fix(deploy): 优化 educloud-order 的 Nacos 服务注册凭据
8c6e2e1 fix(order): 完善 JWT JWKS 动态解码器配置与 application.yml 映射
6bd1fc2 fix(order): 添加 spring-cloud-starter-loadbalancer 依赖并优化启动脚本容错
6c56118 docs: 整理并提交 M07 订单中心完整实现与验收交付记录
081d88f feat(admin): 管理端对接真实订单管理列表与订单详情查看
23b2e82 fix(student): 修复防重Token解包、购物车清空路径与订单倒计时展示
791eb42 feat(student): 学生端对接真实订单提单、模拟支付、订单列表与状态联动
d31cad1 feat(order): 网关路由集成与一键启动脚本适配
f9e3943 feat(order): 模拟支付、OrderPaidEvent 广播与 Course 选课联动
4805a17 feat(order): RabbitMQ TTL + 死信超时关单队列与消费监听
138c620 feat(order): 交易订单核心服务与状态机（提单、快照固化、主动取消）
a161e19 feat(order): OpenFeign 远程课程查价与销售快照固化
d22af0f feat(order): 下单防重 Token 机制与 Redis Lua 校验
982e4ba feat(order): 购物车 CRUD 管理模块与状态计算
1f29d96 feat(order): 错误码体系、业务异常与统一全局拦截
ff83ff4 feat(order): 领域实体、状态枚举与 MyBatis-Plus Mappers
d90678d feat(order): 数据库迁移脚本、雪花 ID 与健康检查配置
3fb4d68 feat(order): Maven 父工程与 educloud-order 模块脚手架
```

---

## 7. 踩坑备忘录与避坑原则（⚠️ 接手必读）

1. **Snowflake ID 严禁 Number 化**：
   - 订单号、订单条目 ID、课程 ID 均为 64 位雪花 ID（如 `2091895618182258690`），后端必须标注 `@JsonSerialize(using = ToStringSerializer.class)`，前端 TypeScript 必须声明为 `string`，杜绝 `Number(id)` 导致精度丢失。
2. **统一 API Result 信封解包**：
   - 所有接口返回 `{ code: "SUCCESS", message: "OK", data: T, traceId: "..." }`，前端 `http.ts` 统一解包，避免因直接访问 `resp.data` 与 `resp.data.data` 层级混淆导致空指针。
3. **JDBC URL 字符集参数**：
   - MySQL 连接串必须使用 `characterEncoding=utf8`（Java JDBC 规范），切勿写成 `characterEncoding=utf8mb4`，否则会触发 `Unsupported character encoding` 运行时异常。
4. **Nacos 命名空间与鉴权凭据**：
   - 虚拟机 Nacos 开启了鉴权，微服务注册必须带上用户名密码，且必须指定 `namespace: educloud-local` 与 `group: EDUCLOUD_SERVICES`。
5. **Spring Cloud LoadBalancer 依赖**：
   - 使用 FeignClient 进行服务名负载均衡调用（如 `educloud-course`）时，必须在 POM 中显式引入 `spring-cloud-starter-loadbalancer`。

---

## 8. 下一步接力开发路线图：`M08 educloud-payment`（支付中心）

后续接力工程师可直接启动 **M08** 模块的开发，核心范围与任务分解如下：

1. **模块定位**：`educloud-backend/educloud-payment`（端口：业务 `8093` / 监控 `8094`）。
2. **核心业务与领域模型**：
   - **多渠道收银台**：微信支付、支付宝、聚合收银台、Mock 支付沙箱；
   - **支付单状态机**：`INITIATED`（已发起）➔ `PAYING`（支付中）➔ `SUCCESS`（成功）/ `FAILED`（失败）/ `CLOSED`（已关闭）；
   - **异步回调通知与验签**：处理三方支付异步 Notification 回调，基于 RSA 签名验签，防重放攻击与数据篡改；
   - **对账与退款中心**：支持全额/部分退款申请、退款审核流（关联 `educloud-order` 的 `refund_request`）与日终自动对账批处理；
   - **事务性发件箱（Transactional Outbox）**：支付成功状态落库与发布 `PaymentSucceededEvent` 保持本地强一致事务。
3. **参考设计文档**：
   - 领域与服务划分：[`docs/superpowers/specs/2026-08-18-educloud-services-and-domains.md`](file:///d:/microservice/.worktrees/educloud-backend-foundation/docs/superpowers/specs/2026-08-18-educloud-services-and-domains.md)
   - 执行顺序与门禁：[`docs/superpowers/specs/2026-08-20-educloud-backend-module-execution.md`](file:///d:/microservice/.worktrees/educloud-backend-foundation/docs/superpowers/specs/2026-08-20-educloud-backend-module-execution.md)

---

*祝下一位接力工程师开发顺利！交接人：AI 助手（按用户要求编制），2026-08-24。*

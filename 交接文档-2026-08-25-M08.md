# 交接文档-2026-08-25-M08（educloud-payment 支付中心与多渠道收银台）

> 面向：下一位接手 EduCloud 微服务开发的工程师。  
> 模块：M08 支付中心与多渠道收银台（`educloud-payment`）及前端双端交互——**已全部完成并验收**，本文是交接与继承指引。  
> 生成时间：2026-08-25  

---

## 0. 一句话状态

M08 支付中心**全部功能完成、代码审查安全加固达标、通用门禁全量通过、多渠道 SPI 沙箱联调打通、全链路 E2E 自动化测试 100% 通过**；  
涵盖统一支付 SPI 插件化架构（Mock / Alipay EasySDK / WeChatPay V3）、Redis 10s 分布式锁排他防重放、分位级金额防篡改与 IDOR 水平越权拦截、CAS 乐观锁状态机流转、Transactional Outbox 本地发件箱可靠事件广播、下游订单履约与课程选课自动撤销（`REVOKED`）、日终双向对账比对引擎与 4 类差错人工平账、学生端多渠道收银台与管理端财务退款对账中心；  
全套 **9 个微服务模块** `mvn test` 执行通过率 100%，前端双门户 TypeScript / Vite 生产构建 0 错误；下一步可直接启动 **M09（educloud-live 直播中心）** 或 **M10（educloud-notification 通知中心）**。

---

## 1. 代码与文档位置

| 项目 | 位置 |
|---|---|
| 唯一权威工作区 | `D:\microservice`（**main 分支**，git 仓库） |
| 虚拟机对应路径 | `/root/educloud/.worktrees/educloud-backend-foundation`（192.168.100.136） |
| 远端仓库 | GitHub `https://github.com/ljccc2025/educloud.git` |
| M08 源码目录 | `educloud-backend/educloud-payment/` |
| M08 数据库迁移 | `deploy/sql/payment/`（V000~V002）+ `deploy/sql/user/V008__payment_permissions.sql` |
| 历史交接文档 | `交接文档-2026-08-24-M07.md`（订单中心）、`交接文档-2026-08-25-代码审查修复.md` |
| 本交接文档 | `交接文档-2026-08-25-M08.md` |

**工作区纪律**：所有开发必须保证主工程可独立编译测试通过；改完提交并推送至 GitHub `main` 分支。

---

## 2. 模块范围与交付物

M08 = 支付中心全链路：**统一支付 SPI 插件体系 / Mock 沙箱 / 支付宝 EasySDK / 微信支付 V3 / 收银台统一调起 / 异步回调安全验签 / 金额防篡改 / Redis 防重放 / CAS 状态机 / Transactional Outbox 可靠事件广播 / 下游订单 PAID 与退款 REFUNDED 履约 / 课程开课与注销选课 / 退款申请与财务两级审核 / 渠道原路退款 / 日终双向对账引擎 / 4 类差错单识别 / 管理端人工平账工作台 / 学生端多渠道收银台 / 管理端财务中心**。

### 2.1 核心业务流程与领域架构

1. **统一支付 SPI 插件化架构（Payment SPI）**：
   - 统一契约接口 `PaymentChannelPlugin`，包含统一下单 `initiatePayment`、回调验签 `verifyAndParseCallback`、原路退款 `initiateRefund`、单笔查单 `queryPayment` 与渠道账单下载 `downloadBill`；
   - 实现了三大渠道插件：
     - `MockPaymentPlugin`：沙箱模式，支持毫秒级模拟支付成功与账单生成；
     - `AlipayEasySdkPlugin`：支持 RSA2 验签、Page/QR 网页收银台调起与退款；
     - `WeChatPayV3Plugin`：支持 AEAD_AES_256_GCM 回调解密验签、Native code_url 扫码与退款；
   - `PaymentChannelFactory` 依赖 Spring 容器统一注入，按 `PaymentChannel` 枚举（`MOCK` / `ALIPAY` / `WECHAT`）运行时分发。

2. **收银台服务与防双花 CAS 状态流转**：
   - 学生提单后调用 `POST /api/v1/payments/cashier`，支付中心通过 OpenFeign `OrderClient` 查验订单可付快照；
   - 支付单状态机：`INITIATED`（已初始化）➔ `PAYING`（支付中）➔ `SUCCESS`（成功）/ `FAILED`（失败）/ `CLOSED`（已关闭）；
   - 利用 `PaymentOrderMapper.updateStatusToSuccessCas` 执行原子 CAS 更新（`WHERE id = ? AND status = 'PAYING' AND deleted = 0`），防止并发双花。

3. **回调安全验签、防重放与金额防篡改**：
   - **防并发重放**：进入回调即对 `educloud:payment:lock:callback:{channel}:{paymentOrderId}` 施加 Redis 10s 分布式排他锁；
   - **金额防篡改**：严格校验 `verifyResult.getAmountCents() == paymentOrder.getAmountCents()`，一旦不一致记录安全告警日志并抛出 `AMOUNT_MISMATCH` 异常；
   - **幂等防护**：全量回调记录写入 `payment_callback_log` 并计算 SHA-256 请求哈希指纹，终态订单安全返回成功响应。

4. **Transactional Outbox 本地发件箱与 RabbitMQ 可靠广播**：
   - 支付成功与退款成功在本地主事务中分别写入 `PaymentSucceededEvent` 和 `PaymentRefundedEvent` 到 `payment_outbox_event`；
   - `PaymentOutboxRelay` 以 `@Scheduled(fixedDelay = 1000)` 扫描待发送事件，利用 CAS 抢占 `updateStatusCas(id, PENDING, SENDING)` 投递到 `educloud.payment.exchange`；
   - 下游消费者响应：
     - `educloud-order`（`PaymentEventsConsumer`）：将业务订单 CAS 更新为 `PAID` / `REFUNDED`；
     - `educloud-course`（`PaymentRefundListener`）：监听到退款事件后调用 `revokeCourseEnrollmentByOrder`，将学员选课状态置为 `REVOKED` 注销学习权益。

5. **退款申请、财务审核与原路退款**：
   - 学员/系统发起 `POST /api/v1/payments/refunds/apply`，拦截超出最大可退余额的非法申请；
   - 财务管理员在管理端审核 `POST /api/v1/admin/payments/refunds/{id}/audit`；
   - 审核通过后，CAS 更新退款单为 `PROCESSING` 并调用渠道 SPI `initiateRefund` 原路退款；退款成功后 CAS 置为 `SUCCESS` 并写入 Outbox 事件广播。

6. **日终双向对账比对引擎与人工平账工作台**：
   - 管理员触发或日终定时执行 `POST /api/v1/admin/payments/reconciliation/trigger`；
   - 对账引擎拉取指定日期的渠道账单与本地 `payment_order` 流水进行双向匹配；
   - 自动识别并生成 4 类差错单（`reconciliation_diff`）：
     - `LOCAL_MORE`：本地成功但渠道无记录（本地单边）；
     - `CHANNEL_MORE`：渠道有交易但本地无记录（渠道单边）；
     - `AMOUNT_MISMATCH`：两边均存在但金额不一致；
     - `STATUS_MISMATCH`：两边均存在但交易状态不符；
   - 提供人工平账接口 `POST /api/v1/admin/payments/reconciliation/diffs/{id}/resolve`，支持 `MANUAL_REPAIR`（人工补单）、`REFUND_OFFLINE`（线下退款）、`ADJUST_AMOUNT`（调账）与 `IGNORE`（忽略），所有差错解决后批次自动更新为 `RESOLVED`。

---

### 2.2 关键端点清单（经网关 /api/v1）

| 端 | 端点路径 | 请求方式 | 鉴权/权限 | 说明 |
|---|---|---|---|---|
| 学生 | `/payments/cashier` | POST | Bearer Token | 发起收银台支付（支持 MOCK/ALIPAY/WECHAT） |
| 学生 | `/payments/{id}` | GET | Bearer Token | 查询支付单明细与支付状态（含属主越权校验） |
| 学生 | `/payments/{id}/mock-confirm` | POST | Bearer Token | Mock 渠道模拟确认支付成功（开发测试环境） |
| 学生 | `/payments/refunds/apply` | POST | Bearer Token | 发起订单退款申请（含属主与可退余额校验） |
| 渠道 | `/payment-callbacks/MOCK` | POST | 匿名可达 | Mock 渠道异步回调通知接口 |
| 渠道 | `/payment-callbacks/ALIPAY` | POST | 匿名可达 | 支付宝异步回调（Form 表单格式，RSA2 验签） |
| 渠道 | `/payment-callbacks/WECHAT` | POST | 匿名可达 | 微信支付 V3 回调（JSON 格式，AES-GCM 解密） |
| 管理 | `/admin/payments/refunds` | GET | `payment:admin` | 分页查询退款申请列表（支持状态过滤） |
| 管理 | `/admin/payments/refunds/{id}` | GET | `payment:admin` | 查看退款单详情 |
| 管理 | `/admin/payments/refunds/{id}/audit` | POST | `payment:admin` | 财务审核退款（同意并原路退款 / 拒绝） |
| 管理 | `/admin/payments/reconciliation/trigger` | POST | `payment:admin` | 触发日终渠道双向对账批次 |
| 管理 | `/admin/payments/reconciliation/batches` | GET | `payment:admin` | 分页查询对账批次历史 |
| 管理 | `/admin/payments/reconciliation/batches/{id}/diffs` | GET | `payment:admin` | 查看对账批次下的差错单明细 |
| 管理 | `/admin/payments/reconciliation/diffs/{id}/resolve` | POST | `payment:admin` | 人工平账处理（补单/线下退款/调账/忽略） |

---

### 2.3 数据库表结构与脚本

- `deploy/sql/payment/V000__technical_tables.sql`：MyBatis-Plus 分布式雪花序列与 Outbox 发件箱表；
- `deploy/sql/payment/V001__init_payment_schema.sql`：
  - `payment_order`：统一支付单据表（带金额、渠道、状态、版本号）；
  - `payment_transaction`：支付/退款交易流水表；
  - `payment_callback_log`：第三方回调通知审计日志表；
  - `payment_refund`：退款申请与审核明细表；
  - `reconciliation_batch`：对账批次统计表；
  - `reconciliation_diff`：对账差错明细与平账记录表；
  - `payment_outbox_event`：Transactional Outbox 本地发件箱表；
- `deploy/sql/payment/V002__payment_seed_data.sql`：预置测试支付单、流水与差错数据；
- `deploy/sql/user/V008__payment_permissions.sql`：为财务与管理角色注入 `payment:create/view/refund/reconcile/admin` 权限码（131-135）。

---

## 3. 技术栈与微服务架构规划

- **核心技术栈**：Java 17 / Spring Boot 3.2.5 / Spring Cloud 2023.0.1 / MyBatis-Plus 3.5.12 / OpenFeign / RabbitMQ / Redis / MySQL 8.0 / React 18 / Tailwind CSS。
- **微服务端口规划（双端口标准模式）**：
  - `educloud-gateway`：`8080`（业务） / `8081`（监控）
  - `educloud-user`：`8082`（业务） / `8083`（监控）
  - `educloud-content`：`8085`（业务） / `8086`（监控）
  - `educloud-file`：`8087`（业务） / `8088`（监控）
  - `educloud-course`：`8089`（业务） / `8090`（监控）
  - `educloud-order`：`8091`（业务） / `8092`（监控）
  - `educloud-payment` (M08)：`8093`（业务） / `8094`（监控）
  - 前端三端：`5173`（学生） / `5174`（教师） / `5175`（管理）

---

## 4. 环境信息与运维指南

| 项 | 说明 / 值 |
|---|---|
| 虚拟机 IP | `192.168.100.136`（Rocky Linux 8.9，root 用户 / 密码 `1`） |
| 代码文件路径 | `/root/educloud/.worktrees/educloud-backend-foundation` |
| 一键启动脚本 | `bash deploy/scripts/start-dev.sh`（已适配 `educloud-payment` 启动与 8094 就绪探针） |
| 预置账号（学生） | `fe_demo_10` / `FeDemo@2026`（Snowflake ID: `2091648316809035778`） |
| 预置账号（教师） | `demo_teacher@educloud.cn` / `EduCloud@2026`（ID: `9000000000000000001`） |
| 预置账号（管理） | `demo_admin` / `EduCloud@2026`（ID: `9000000000000000002`） |

---

## 5. 验证与验收记录

1. **后端全量编译与单元测试**：
   - 9 个微服务模块（`common`, `gateway`, `user`, `file`, `course`, `content`, `order`, `payment`）执行 `mvn test` 全部 **100% BUILD SUCCESS**。
2. **全链路 E2E 自动化测试（`PaymentFlowIntegrationTest.java`）**：
   - 阶段 1 & 2：收银台多渠道下单与二维码生成；
   - 阶段 3 & 4：回调签名解析、Redis 排他锁与金额防篡改告警；
   - 阶段 5 & 6：发件箱事件可靠落库与 RabbitMQ 消息广播；
   - 阶段 7：退款申请、总额边界拦截、财务审核通过与渠道原路退款；
   - 阶段 8 & 9：日终对账比对（识别 4 类差错）与人工平账闭环。
   - **结果：9 大阶段 100% 全部断言通过！**
3. **前端构建验证**：
   - `student-portal`、`admin-portal` 执行 `npm run build`，**TypeScript 检查 0 error，顺利输出 dist**。

---

## 6. 近期 Git 提交记录（供追溯）

```text
c9cd190 fix(payment): resolve code review findings for IDOR checks, safe enum parsing and prod security hardening
0d655f0 feat(payment): deliver M08 payment center with multi-channel SPI, reconciliation and refund workflows
f61c0c8 test(order): align mockito argument matchers to fix compilation errors
```

---

## 7. 踩坑备忘录与避坑原则（⚠️ 接手必读）

1. **金额存储必须使用 Long（分位）**：
   - 严禁在数据库与实体中直接使用 `Double` 或 `Float` 存储金额。全链路统一使用 `Long amountCents`（分），避免浮点数精度截断问题；接口层对元转换使用 `BigDecimal.setScale(2, RoundingMode.HALF_UP)`。
2. **Jackson JSR310 序列化配置**：
   - 当在非 Spring 上下文（如独立单测或离线工具）中手动实例化 `ObjectMapper` 时，必须显式调用 `objectMapper.findAndRegisterModules()`，否则序列化包含 `LocalDateTime` 的 Outbox 事件会抛出 `InvalidDefinitionException`。
3. **MyBatis-Plus BaseMapper 重载二义性**：
   - MyBatis-Plus 3.5.12 的 `BaseMapper` 同时定义了 `insert(T)` 和 `insert(Collection<T>)`，在 Mockito 中编写 `verify(mapper).insert(any())` 会因泛型擦除报编译器方法调用二义性错误，必须写为 `verify(mapper).insert(any(PaymentOutboxEventEntity.class))`。
4. **IDOR 水平越权防御规范**：
   - 所有根据 ID 获取单据明细或执行敏感动作（如退款、模拟支付）的端点，必须显式比对 `userId != null && !userId.equals(order.getUserId())`，非管理员角色严禁跨租户/跨学员操作。
5. **分页入参必须设上限**：
   - 所有暴露给前端的分页接口，必须限制 `int safeSize = Math.min(Math.max(size, 1), 100);`，杜绝恶意传入超大 `size` 导致 OOM。

---

## 8. 下一步接力开发路线图：`M09 educloud-live` 或 `M10 educloud-notification`

后续接力工程师可直接从以下模块二选一启动后续建设：

1. **选项 A：`M09 educloud-live`（直播互动中心与 WebSocket 教室）**：
   - **模块定位**：`educloud-backend/educloud-live`（端口：业务 `8095` / 监控 `8096` / WebSocket `8095`）；
   - **核心范围**：直播间生命周期管理（创建/开启/推流/禁言/结束）、基于 Netty/Spring WebSocket 的弹幕聊天室与实时白板信令同步、Redis Pub/Sub 房间跨节点广播。
2. **选项 B：`M10 educloud-notification`（消息与通知中心）**：
   - **模块定位**：`educloud-backend/educloud-notification`（端口：业务 `8097` / 监控 `8098`）；
   - **核心范围**：站内信系统、邮件/短信通道聚合、课程开课/作业提醒/退款成功多渠道订阅与即时推送。

---

*祝下一位接力工程师开发顺利！交接人：AI 助手（按用户要求编制），2026-08-25。*

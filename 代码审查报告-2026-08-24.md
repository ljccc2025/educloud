# 代码审查报告

- **审查对象**：`d:\microservice\educloud-backend` 全部 7 个后端模块主源码（common / gateway / user / file / course / content / order），约 484 个 `.java` 文件
- **审查日期**：2026-08-24
- **审查方式**：逐模块全量源码阅读 + P0/P1 高危项在源码中逐条复核实锤
- **审查维度**：逻辑错误、空指针、越界、资源泄漏、并发安全、异常处理、边界条件、类型转换、API 误用、隐藏生产隐患
- **严重级别定义**：P0 致命（越权/资损/数据丢失）｜P1 严重（安全缺陷/一致性破坏）｜P2 一般（竞态/可用性/语义失真）｜P3 建议（健壮性/可维护性）

## 总体评价

整体工程化水平较高：雪花 ID 字符串化、双端口模式、Outbox 表、CAS 状态机、内部接口过滤器等基础设施齐全；网关的 `/internal/**` 三层防护、JWT 密钥白名单、BCrypt 与刷新令牌轮转实现规范。但**内容服务（content）与订单服务（order）存在多个可直接造成资损与越权的致命/严重缺陷**，其中 2 个 P0 已在源码中逐行复核确认，建议在任何对外演示或真实用户接入前优先修复。

## 问题统计

| 级别 | 数量 | 主要分布 |
|---|---|---|
| P0 致命 | 2 | content（审核接口无鉴权、付费课件任意登录用户可下载） |
| P1 严重 | 20 | order×7、user×4、content×4、course×3、common×2 |
| P2 一般 | 30 | 并发竞态、长事务、幂等缺陷、语义失真 |
| P3 建议 | 34 | 健壮性、N+1、防御性编码 |
| **合计** | **86** | |

## 建议修复优先级

1. **立即修复（P0）**：BUG-001、BUG-002 —— 内容审核越权与付费内容裸奔
2. **本迭代必修（P1 交易/权益类）**：BUG-016~022（订单）、BUG-034~037（用户）、BUG-051~053（开课丢失）
3. **本迭代应修（P1 其余）**：BUG-003~006、BUG-079~080
4. **下迭代修复（P2）**：按模块批量处理
5. **择机优化（P3）**：随常规迭代消化

---

# 一、educloud-content（15 项）

## BUG-001｜P0 致命｜管理端内容审核接口完全缺失鉴权

- **问题位置**：`educloud-content/src/main/java/com/educloud/content/controller/ContentAdminController.java`（全文，尤其 L46-63 `approveAudit`/`rejectAudit`）；`config/SecurityConfig.java:46`
- **问题描述**：控制器导入了 `PreAuthorize` 但全文未使用任何鉴权注解；`SecurityConfig` 仅配置 `anyRequest().authenticated()`，服务层 `ContentAuditService` 亦无权限校验。`approveAudit`/`rejectAudit` 中 `adminId` 还有 `jwt != null ? ... : 0L` 的兜底写法。
- **触发条件**：任何持有有效 JWT 的用户（包括学生、普通教师）直接调用 `/api/v1/admin/content-audits/**`。
- **潜在影响**：学生可查看含完整内容快照（`snapshotJson`）的全部审核单；教师可"自己提交、自己批准"绕过整个内容审核流程直接发布内容，审核机制形同虚设。
- **建议修复方案**：控制器方法添加 `@PreAuthorize("hasAuthority('content:audit')")`（与 `jwtAuthenticationConverter` 的 `permissions` claim 对应）；删除 `0L` 兜底，未认证直接失败。

## BUG-002｜P0 致命｜付费课件下载链接对任意登录用户开放

- **问题位置**：`educloud-content/src/main/java/com/educloud/content/service/CoursewareAccessService.java:28-35`；`config/SecurityConfig.java:40-44`（`download-url` 路由 `permitAll`）
- **问题描述**：下载链接接口准入逻辑为 `isStudent = userId != null`，即只要携带任意有效用户 JWT 即放行，全程无订单/购买/报名校验（未调用 order 服务）。
- **触发条件**：任意已登录用户（未购买该课程）请求任意非免费课件的 `GET /api/v1/coursewares/{id}/download-url`。
- **潜在影响**：付费课件的文件下载直链可被任何登录用户免费获取，核心营收内容的可见性控制完全失效，等价于全站付费内容裸奔。
- **建议修复方案**：调用 order 服务内部接口校验 `userId` 对 `cw.getCourseId()` 的购买状态（教师/管理员豁免）；未购买且非 `freePreview` 时抛 `COURSEWARE_ACCESS_DENIED`；匿名用户仅允许 `freePreview` 内容。

## BUG-003｜P1 严重｜下载接口不校验课件所属版本是否已发布

- **问题位置**：`CoursewareAccessService.java:23-30`
- **问题描述**：仅检查 `status != DELETED`，未校验 `contentRevisionId` 是否为课程的 `publishedRevisionId`；草稿版本（未提交或审核未通过）的课件同样是 `ACTIVE` 状态。
- **触发条件**：教师新建草稿课件后，他人获知课件 ID 即请求 download-url。
- **潜在影响**：未发布、被驳回的草稿内容可被外部下载，与公共章节接口只暴露已发布版本的行为不一致。
- **建议修复方案**：校验 `cw.getContentRevisionId().equals(content.getPublishedRevisionId())`，否则拒绝（教师/管理员豁免）。

## BUG-004｜P1 严重｜JWT audience 校验恒通过

- **问题位置**：`educloud-content/src/main/java/com/educloud/content/security/ContentJwtValidator.java:36-53`（已逐行复核，L52 兜底返回 `success()`）
- **问题描述**：`aud` 匹配本服务/内部 audience 时返回 success，**不匹配时 L52 依然返回 `success()`**，任意 audience（甚至无 aud）的令牌都通过校验。
- **触发条件**：持有同一 issuer 签发、受众为其他服务的合法 JWT 访问本服务。
- **潜在影响**：跨服务令牌复用，单令牌泄露的爆炸半径扩大到整个 content 服务。
- **建议修复方案**：`aud` 不包含本服务 audience 时返回 `failure("audience")`。

## BUG-005｜P1 严重｜教师端无课程归属校验，横向越权成立

- **问题位置**：`security/TeacherAccessGuard.java:14-29`（已复核）；`service/ChapterService.java:65-115`、`CoursewareService.java:78-130`、`ContentRevisionService.java:32-100`
- **问题描述**：守卫只校验角色/权限，不校验该教师是否为课程负责人；`teacherId` 参数在 `updateChapter`、`deleteChapter`、`updateCourseware`、`deleteCourseware`、`submitReview`、`withdrawReview` 中全部未被用于归属判断。
- **触发条件**：教师 B 用自己的合法令牌调用 `/api/v1/teacher/chapters/{教师A课程的章节ID}` 等接口。
- **潜在影响**：任意教师可篡改、删除其他教师任意课程的草稿内容，可替他人提交或撤回审核。
- **建议修复方案**：操作前通过 course 服务内部接口校验 `courseId` 归属教师与当前用户一致（管理员豁免），不一致抛 `TEACHER_ACCESS_DENIED`。

## BUG-006｜P1 严重｜审核批准时手工版本号与乐观锁冲突，发布静默失败

- **问题位置**：`service/ContentAuditService.java:91-94`；`entity/CourseContentEntity.java:21-22`（`@Version` 已复核）；`config/MybatisPlusConfig`（注册了 `OptimisticLockerInnerInterceptor`）
- **问题描述**：`aggregateVersion` 标注 `@Version`，而 `approveAudit` 又手动 `setAggregateVersion(+1)`；拦截器以实体当前值（已 +1）作为 WHERE 条件版本再 +1，生成 `WHERE aggregate_version = v+1`，数据库行仍为 `v`，更新影响行数为 0 且返回值未检查。
- **触发条件**：任何一次审核批准操作（常规路径即触发）。
- **潜在影响**：版本被标记 `PUBLISHED`、审核单变 `APPROVED`、领域事件已发，但 `published_revision_id` 未更新——课程对外仍展示旧版本，数据严重不一致且无告警。
- **建议修复方案**：删除手工 +1，交由 `@Version` 拦截器自增；检查 `updateById` 返回值，为 0 时抛异常回滚事务。

## BUG-007｜P2 一般｜审核状态机缺少并发保护

- **问题位置**：`ContentAuditService.java:54-138`；`ContentRevisionService.java:79-100`
- **问题描述**：`approveAudit`/`rejectAudit` 先 `selectById` 判断 `PENDING` 再无条件 `updateById`，无条件更新或行锁；`withdrawReview` 同理不校验 `submission.status`。
- **触发条件**：两管理员同时批准同一审核单；或教师撤回与管理员批准并发。
- **潜在影响**：重复发布事件、状态互相覆盖（`publishedRevisionId` 指向 `WITHDRAWN` 版本），产生悬挂发布引用。
- **建议修复方案**：改为条件更新 `UPDATE ... SET status=? WHERE id=? AND status='PENDING'` 并检查影响行数。

## BUG-008｜P2 一般｜学习进度上报非原子读-改-写，重复行导致列表 500

- **问题位置**：`service/CourseProgressService.java:59-96`；`service/CourseContentService.java:185-186`
- **问题描述**：`watchedSeconds` 在应用层累加后 `updateById`，实体无 `@Version`；首次插入为先查后插。一旦并发产生重复进度行，`buildChapterTree` 的 `Collectors.toMap(getCoursewareId, ...)` 抛 `IllegalStateException`。
- **触发条件**：同一学生双标签页/网络重试并发心跳。
- **潜在影响**：进度丢失更新；公共章节列表接口 500。
- **建议修复方案**：改为 `UPDATE ... SET watched_seconds = watched_seconds + #{delta} WHERE ...` 原子更新；加唯一索引；`toMap` 增加合并函数兜底。

## BUG-009｜P2 一般｜进度上报无购买校验且完成状态可客户端伪造

- **问题位置**：`CourseProgressService.java:41-53、73-87`
- **问题描述**：接口未验证学生是否购买该课程；直接采信请求体 `completed: true`，可在 `positionSeconds=0` 时上报完成，绕过 60 秒 delta 防作弊上限。
- **触发条件**：任意已登录学生对任意课件上报 `{"completed": true, ...}`。
- **潜在影响**：未购课程可刷出 100% 进度；若下游以进度作为权益/证书依据则进一步放大。
- **建议修复方案**：服务端忽略客户端 `completed`，仅按 `watchedSeconds >= duration*0.85` 判定；上报前校验购买关系。

## BUG-010｜P2 一般｜Outbox 序列号在 REPEATABLE READ 下可重复

- **问题位置**：`messaging/OutboxWriter.java:34-38`；`mapper/OutboxSequenceMapper.java:13-17`
- **问题描述**：`UPDATE ... SET last_value = last_value + 1` 为当前读持行锁，但随后的普通 `SELECT last_value` 走 MVCC 快照读；锁等待场景下可能读到旧值。
- **触发条件**：多个发事件操作并发写 Outbox 且发生锁等待。
- **潜在影响**：`outbox_event.source_sequence` 重复，破坏下游按序消费/去重的单调序保证。
- **建议修复方案**：改为 `UPDATE ... SET last_value = LAST_INSERT_ID(last_value + 1)` + `SELECT LAST_INSERT_ID()`（会话内无快照问题）。

## BUG-011｜P2 一般｜提交审核无唯一性防护且无归属校验

- **问题位置**：`ContentRevisionService.java:53-76`
- **问题描述**：`submitReview` 先 `selectOne` 后 insert/update，无唯一索引防护；未校验 `teacherId` 与版本/课程归属。
- **触发条件**：并发点击提交；或教师 B 提交教师 A 的 DRAFT 版本。
- **潜在影响**：同一版本产生两条 `PENDING` 审核单；配合 BUG-005 构成他人内容被任意提审。
- **建议修复方案**：`content_revision_id` 加唯一索引，捕获冲突转更新；补充归属校验。

## BUG-012｜P2 一般｜审核列表分页无上限，可构成分页型 DoS

- **问题位置**：`ContentAuditService.java:32-43`（listAudits）
- **问题描述**：`size` 直接传入 `new Page<>(page, size)`，可传超大值；`status` 无枚举校验（非法值静默返回空页）。
- **触发条件**：客户端请求 `size=10000000`。
- **潜在影响**：单次查询拉取海量 `snapshotJson` 大字段，内存压力/慢查询。
- **建议修复方案**：强制 `page >= 1`、`1 <= size <= 100`；`status` 枚举解析，非法返回 400。

## BUG-013｜P2 一般｜课件文件引用无存在性/归属校验，删除不解绑

- **问题位置**：`CoursewareService.java:49、91-92、115-130`
- **问题描述**：`fileId` 直接采信客户端传入值，未通过 `FileClient` 校验文件存在、未删除及归属；删除课件仅置 `DELETED`，未通知 file 服务解除引用。
- **触发条件**：教师填入不存在/他人的 `fileId`；删除含文件课件。
- **潜在影响**：悬挂引用与越权引用他人文件；被删课件文件永久滞留存储（孤儿文件）。
- **建议修复方案**：保存时校验 `fileId` 存在且属主匹配；删除时发 `CoursewareDeleted` 事件异步解绑。

## BUG-014｜P3 建议｜"新建草稿"语义实际返回旧草稿

- **问题位置**：`CourseContentService.java:109-112`（cloneNewDraft）
- **问题描述**：`cloneNewDraft` 直接委托 `getOrCreateDraft`，已存在 DRAFT 时原样返回旧草稿。
- **触发条件**：教师在已有草稿时点击"新建版本"。
- **潜在影响**：接口语义与行为不符，易造成数据误操作假象。
- **建议修复方案**：存在进行中草稿时返回 409，或提供明确的"丢弃草稿重建"流程。

## BUG-015｜P3 建议｜内容根记录并发重复插入与过期时间硬编码

- **问题位置**：`CourseContentService.java:54-66`（getOrCreateDraft 先查后插）；`CoursewareAccessService.java:51`（`expiresAt` 硬编码 15 分钟）
- **问题描述**：并发下同一 `courseId` 可能插入两条 `course_content` 根记录（无唯一索引），后续 `selectOne` 抛 `TooManyResultsException`；`expiresAt` 未取 file 服务实际返回的过期时间。
- **触发条件**：同一课程首次并发打开草稿；file 服务 grant TTL 非 900 秒。
- **潜在影响**：接口 500；过期时间展示失真误导前端缓存。
- **建议修复方案**：`course_content.course_id` 加唯一索引并捕获冲突重查；`expiresAt` 从 grant 结果透传。

---

# 二、educloud-order（18 项）

## BUG-016｜P1 严重｜支付不校验订单过期时间，15 分钟窗口可被突破

- **问题位置**：`service/impl/OrderServiceImpl.java:216-262`（mockPay，已复核）；`mapper/TradeOrderMapper.java:22-28`（CAS SQL）
- **问题描述**：支付仅校验 `status = PENDING_PAYMENT`，不校验 `expires_at`；CAS SQL 亦无 `AND expires_at > NOW(3)`。`OrderErrorCode.ORDER_EXPIRED` 已定义但全工程零引用，说明过期校验被整体遗漏。关单完全依赖 15 分钟死信消息这一条路径。
- **触发条件**：延时消息发送失败（见 BUG-018）或 MQ 积压延迟到达时，订单超期仍处 PENDING。
- **潜在影响**：已过期订单被成功支付，订单可无限期挂起并支付，破坏交易窗口语义。
- **建议修复方案**：mockPay 增加 `expires_at` 校验抛 `ORDER_EXPIRED`；CAS SQL 追加 `AND expires_at > NOW(3)` 双保险。

## BUG-017｜P1 严重｜已支付事件发送失败被吞，付款后永不开课

- **问题位置**：`messaging/OrderEventPublisher.java:21-30`（已复核，catch 仅记日志）；`OrderServiceImpl.java:256-259`（事务内调用）
- **问题描述**：`publishOrderPaid` 发送失败仅 `log.error` 吞掉；调用发生在 `@Transactional` 提交之前。工程已有 `outbox_event` 表与 `OutboxEventMapper` 但从未写入——outbox 模式是半成品。
- **触发条件**：① CAS 置 PAID 后、事务提交前 MQ 发送失败 → 事件永久丢失；② MQ 发送成功但事务提交失败 → 发出"未支付订单"的幻影已支付事件。
- **潜在影响**：用户已付款但课程永不开通且无对账补偿；或下游收到幻影事件错误开课。交易与履约不一致，资损/客诉级。
- **建议修复方案**：启用既有 outbox 表：事务内写 `outbox_event`，独立 relay 发送；或至少 `afterCommit` 发送 + 失败落重试表。

## BUG-018｜P1 严重｜延时关单消息发送失败被吞且无兜底扫描

- **问题位置**：`messaging/OrderDelayProducer.java:27-30`；`OrderServiceImpl.java:144-151`
- **问题描述**：延时关单消息在 `createOrder` 事务内发送，异常被吞后事务照常提交，无 outbox、无定时扫描超时订单的兜底任务；且消息在事务提交前发出，事务回滚会产生幽灵订单消息。
- **触发条件**：下单瞬间 RabbitMQ 不可用/网络抖动。
- **潜在影响**：该订单永远收不到关单消息，结合 BUG-016 形成永久可支付的挂起订单。
- **建议修复方案**：`afterCommit` 发送 + 失败重试表；补充定时任务扫描 `status=PENDING_PAYMENT AND expires_at < NOW()` 兜底关单。

## BUG-019｜P1 严重｜提单大事务混杂 Feign/Redis/MQ 远程调用

- **问题位置**：`OrderServiceImpl.java:60-154`（createOrder）
- **问题描述**：Redis 幂等消费、Feign 同步 HTTP 查价（循环逐课程调用）、数据库写入、MQ 发送全部在同一事务中；Redis/MQ 操作不受回滚保护；Feign 未配置超时与熔断。
- **触发条件**：course 服务响应缓慢（默认 read timeout 10s）。
- **潜在影响**：慢 RPC 期间长期持有数据库连接，提单高峰时连接池耗尽、整个订单服务雪崩；回滚时 token 已删、消息已发，无法一致回退。
- **建议修复方案**：Feign 校验移出事务（先校验组装再开事务写库）；配置显式超时与熔断；循环单查改批量接口。

## BUG-020｜P1 严重｜购物车响应为硬编码假数据，金额恒为 0

- **问题位置**：`service/impl/CartServiceImpl.java:109-139`（toResponse/getCartSummary）
- **问题描述**：`unitPrice` 恒为 `BigDecimal.ZERO`、`courseTitle` 恒为 `"课程 " + courseId`、`coverFileId` 恒为 `null`、`isOnSale` 恒为 `true`，未调用课程服务；`selectedAmount` 恒等于 0。
- **触发条件**：任何用户查看购物车。
- **潜在影响**：交易核心链路金额展示错误：前端合计 0 元、已下架课程显示"在售"，与实际下单金额不一致，误导用户。
- **建议修复方案**：汇总时通过 `CourseClient` 批量拉取价格与上架状态填充；已下架标记 `isOnSale=false` 且不计入金额。

## BUG-021｜P1 严重｜订单金额无合法性校验，负价格可入库

- **问题位置**：`OrderServiceImpl.java:98-100`；`feign/dto/CourseSalesSnapshotDto.java:39-41`（isPurchasable）
- **问题描述**：`price` 直接累加为 `payableAmount`，不校验非负与 scale；高精度价格写入 `DECIMAL(10,2)` 会截断/报错；`isPurchasable()` 中 `isOnSale == null` 被当作"在售"。
- **触发条件**：上游课程数据异常（脏数据、字段缺失）。
- **潜在影响**：负金额/错金额订单入库并驱动下游履约，资损级缺陷。
- **建议修复方案**：校验 `price != null && price.signum() >= 0 && price.scale() <= 2`；`isPurchasable()` 改为 `Boolean.TRUE.equals(isOnSale)`。

## BUG-022｜P1 严重｜mock-pay 对学生身份暴露且无环境门控

- **问题位置**：`controller/OrderStudentController.java:62-68`；`OrderServiceImpl.java:216-262`
- **问题描述**：`POST /api/v1/orders/{id}/mock-pay` 无任何环境开关，学生无需真实支付即可将订单置为 PAID 并触发履约。
- **触发条件**：该服务部署到非 local 环境。
- **潜在影响**：免费获取付费课程，直接资损；缺少防误部署硬约束。
- **建议修复方案**：入口校验 `environment == local/dev` 否则 403/404；或 `@ConditionalOnProperty` 仅在开发环境注册端点。

## BUG-023｜P2 一般｜提单失败后幂等 Token 不归还，重试被误判重复提交

- **问题位置**：`service/impl/IdempotencyServiceImpl.java:21`；`OrderServiceImpl.java:67`
- **问题描述**：token 在业务执行前即被 Lua 原子消费（删除），且不参与 Spring 事务；后续流程失败回滚后 token 不归还。`TOKEN_TTL=10 分钟` 小于订单 15 分钟有效期。
- **触发条件**：首次提单因课程服务异常/事务回滚失败后立即重试。
- **潜在影响**：正常重试收到 409 `DUPLICATE_ORDER_SUBMISSION`，但实际从未产生订单，错误码语义与事实不符。
- **建议修复方案**：事务失败时在 `afterCompletion` 回补 token；或将幂等记录落 `idempotency_record` 表随事务提交；TTL 调整为 ≥ 订单有效期。

## BUG-024｜P2 一般｜关单消费者无异常处理与死信，关单无领域事件

- **问题位置**：`messaging/OrderDelayConsumer.java:22-50`；`config/RabbitOrderConfig.java:61-64`
- **问题描述**：默认 AUTO-ACK 下 DB 瞬时异常触发消息无限重投；`ORDER_CANCEL_QUEUE` 无 `x-dead-letter-exchange`，毒消息造成重投风暴；关单后未发布"订单已关闭"事件。
- **触发条件**：消费时 DB 抖动；收到格式异常消息。
- **潜在影响**：消息堆积/重投风暴；下游无法感知订单关闭。
- **建议修复方案**：配置 DLX 与重试上限；业务异常分类处理；关单成功后发布 OrderClosed 事件。

## BUG-025｜P2 一般｜内部接口鉴权薄弱：非常量时间比较 + permitAll + 默认密钥

- **问题位置**：`config/InternalApiFilter.java:42-51`；`config/SecurityConfig.java:43`；`resources/application.yml:52`
- **问题描述**：① `configuredSecret.equals(internalTokenHeader)` 非常量时间比较，存在时序侧信道；② `/internal/**` 直接 `permitAll()`，安全完全押注单个过滤器；③ 默认密钥 `educloud-internal-secret` 硬编码在配置默认值中。
- **触发条件**：生产未覆盖 `EDUCLOUD_ORDER_INTERNAL_SECRET`，或攻击者直达服务端口绕过网关。
- **潜在影响**：知道默认密钥/端口者可枚举读取全部订单的应付与履约快照（金额、学员、课程明细）。
- **建议修复方案**：改用 `MessageDigest.isEqual`；启动时校验 secret 非默认值否则拒绝启动；`/internal/**` 纳入认证路径。

## BUG-026｜P2 一般｜Feign 故障被伪装成"课程未上架"

- **问题位置**：`OrderServiceImpl.java:347-353`（fetchAndValidateCourse）
- **问题描述**：所有 Feign 异常（超时、5xx、连接拒绝）一律转译为 `COURSE_NOT_ON_SALE`（HTTP 400），基础设施故障被伪装成业务拒绝；`CourseClient` 无 fallback 与熔断。
- **触发条件**：course 服务不可用或超时。
- **潜在影响**：错误码语义失真误导排障；用户得到不可重试的 400。
- **建议修复方案**：区分 `FeignException` 状态：4xx 转业务码，5xx/超时转 503 类可重试码；配置超时与熔断。

## BUG-027｜P2 一般｜同一课程可重复下未支付订单，购物车结算集合无锁

- **问题位置**：`OrderServiceImpl.java:70-88`（createOrder 分支）
- **问题描述**：未校验"该课程已有未支付订单"；购物车分支查询选中项后未加锁即提交，勾选变更与提单并发时可能结算到非预期集合。
- **触发条件**：同一课程连续快速下两单；勾选变更与提交并发。
- **潜在影响**：同一课程重复付款（两单都能支付并各发一次履约事件），依赖下游去重；结算集合与用户预期不符。
- **建议修复方案**：创建前校验同课程未支付订单；购物车结算对选中行加锁后复核。

## BUG-028｜P3 建议｜内部令牌跳过结构校验，JWKS 不支持刷新

- **问题位置**：`security/OrderJwtValidator.java:63-65`；`security/JwtDecoderConfiguration.java`（`ImmutableJWKSet` 启动加载一次）
- **问题描述**：内部 audience 令牌直接 `return success()`，跳过 `sub`/`sid`/`userType`/roles 结构校验；密钥轮换必须重启服务（`JwksState.lastLoadedTime` 无消费方）。
- **触发条件**：内部令牌签发逻辑出现缺陷；JWKS 轮换。
- **潜在影响**：内部通道校验深度不足；密钥轮换无法平滑过渡。
- **建议修复方案**：内部分支至少校验 `clientId` 受限字符串；改用可刷新的 JWKSource 或定时重载。

## BUG-029｜P3 建议｜加购 check-then-insert 竞态 500，重复加购强制选中

- **问题位置**：`CartServiceImpl.java:29-52`（addItem）
- **问题描述**：并发添加同一课程可能双插入，靠 `uk_student_course` 兜底抛 `DuplicateKeyException` 但未捕获转译（500）；重复添加会把已取消勾选的项强制置回 `selected = true`。
- **触发条件**：多端/快速双击加购。
- **潜在影响**：偶发 500；用户取消勾选状态被意外恢复。
- **建议修复方案**：`INSERT ... ON DUPLICATE KEY UPDATE` 或捕获冲突转更新；强制选中行为与产品确认。

## BUG-030｜P3 建议｜订单列表 N+1 查询且分页无上限

- **问题位置**：`OrderServiceImpl.java:186-191、326-331`；`OrderStudentController.java:38-39`；`OrderAdminController.java:25-26`
- **问题描述**：每条订单单独查明细项（N+1）；`page`/`size` 无上下限校验，`size=100000` 可触发全量拉取 + 十万次子查询。
- **触发条件**：大分页调用。
- **潜在影响**：慢查询与连接占用，低成本 DoS 面。
- **建议修复方案**：收集 `orderIds` 一次 `IN` 查询明细分组；`size` 限制 1-100。

## BUG-031｜P3 建议｜PaymentEventListener 为死代码且埋重复消费隐患

- **问题位置**：`messaging/PaymentEventListener.java:15-18`
- **问题描述**：无 `@RabbitListener`、无调用点；实现直接委托 `mockPay`，未来接入真实支付消息时对"已支付订单"抛 400 业务异常将导致消息无限重投。
- **触发条件**：后续接入真实支付回调。
- **潜在影响**：遗留代码埋下重投风暴隐患。
- **建议修复方案**：删除或实现为正式监听器：先查状态，已支付静默幂等返回。

## BUG-032｜P3 建议｜异常处理器无兜底，格式不统一且潜在信息泄漏

- **问题位置**：`exception/OrderExceptionHandler.java:26-72`
- **问题描述**：只处理 3 类异常，无 `Exception`/`DataAccessException`/`HttpMessageNotReadableException` 兜底；未预期异常落入容器默认错误页。
- **触发条件**：数据库不可用、非法 JSON 等。
- **潜在影响**：错误响应格式不统一、潜在堆栈泄漏。
- **建议修复方案**：增加 `@ExceptionHandler(Exception.class)` 兜底返回统一 500 结构；补充 400 类映射。

## BUG-033｜P3 建议｜订单超时时长双重定义，易错位

- **问题位置**：`config/RabbitOrderConfig.java:30`（`ORDER_TTL_MS = 900000`）；`OrderServiceImpl.java:48`（`ORDER_EXPIRE_MINUTES = 15`）
- **问题描述**：关单窗口在两处以不同形式独立定义，任一处单独修改即产生"已关单但仍可支付"或"未到期即关单"。
- **触发条件**：任一处被单独调整。
- **潜在影响**：关单窗口与订单过期时间不一致。
- **建议修复方案**：统一为单一配置属性注入两处；差异化时效改用消息级 TTL。

---

# 三、educloud-user（17 项）

## BUG-034｜P1 严重｜管理员人工锁定可被一次成功登录静默解除

- **问题位置**：`service/AuthenticationService.java:123-130`（login/clearLock，已复核）；`service/UserStatusService.java:74-84`（已复核：LOCKED 分支不写 `locked_until`）
- **问题描述**：管理员 `updateStatus` 置 LOCKED 时只写 `status`，`locked_until` 为 NULL；而登录逻辑对 LOCKED 的判定是 `lockedUntil != null && lockedUntil.isAfter(now)` 才拒绝，否则视为"锁定到期"执行 `clearLock` 改回 ACTIVE。
- **触发条件**：管理员封禁账号后，该账号持正确密码登录一次。
- **潜在影响**：人工封禁被一次登录静默解除，风控措施形同虚设，无审计差异提示。
- **建议修复方案**：区分自动锁定与人工锁定：人工 LOCKED 必须写 `locked_until`（或加 `locked_by` 标记）；`lockedUntil==null` 的 LOCKED 视为永久锁定拒绝登录。

## BUG-035｜P1 严重｜服务凭据轮换后旧 secret 永久有效

- **问题位置**：`command/ServiceClientCredentialCommand.java:101-105`（rotate）；`service/ServiceTokenService.java:76-78`
- **问题描述**：`rotate` 将旧凭据置 `GRACE` 后 `updateById`，但其 `expires_at` 为 NULL（插入时未设）；而 `issue` 查询明确放行 `status='GRACE' AND expires_at IS NULL`。
- **触发条件**：凭据轮换后继续使用旧 secret 调用 `/internal/v1/service-tokens`。
- **潜在影响**："24h GRACE 宽限期"承诺失效；凭据泄露后无法通过轮换止血。
- **建议修复方案**：`rotate` 置 GRACE 时同步 `setExpiresAt(now + GRACE_DURATION)`；删除 `issue` 中 `isNull("expires_at")` 放行分支。

## BUG-036｜P1 严重｜内部 bootstrap 密钥空串绕过鉴权

- **问题位置**：`controller/InternalServiceClientBootstrapController.java:43-48`；`resources/application.yml:65`（默认占位 `${EDUCLOUD_USER_INTERNAL_BOOTSTRAP_KEY:}`）
- **问题描述**：校验只判 `bootstrapKey() == null` 不判空串；默认配置绑定为空字符串。攻击者发送空值 `X-Bootstrap-Key:` 头时 `MessageDigest.isEqual("".bytes, "".bytes)` 返回 true，校验通过。
- **触发条件**：部署未设置 `EDUCLOUD_USER_INTERNAL_BOOTSTRAP_KEY`（当前默认即如此）且网络可达该端点。
- **潜在影响**：未认证者可任意创建服务客户端并自行声明 audience/scope 白名单，污染服务间信任体系。
- **建议修复方案**：校验改为 `bootstrapKey == null || bootstrapKey.isBlank()` 即拒绝；启动时密钥为空白即 fail-fast。

## BUG-037｜P1 严重｜会话撤销因 Redis 故障整体回滚，被盗令牌可持续续期

- **问题位置**：`service/SessionRevocationService.java:45-48`（revokeFamily）
- **问题描述**：`revokeFamily` 在同一 `@Transactional` 中先更新 DB 再 `sessionStore.markRevoked`；Redis 写入抛异常时整个事务回滚，DB 侧撤销一并丢失，且不存在注释所称的补偿机制。
- **触发条件**：Redis 短暂不可用期间发生注销、刷新令牌重用检测、改密撤销、账号禁用撤销任一路径。
- **潜在影响**：应撤销的会话族在 DB 中仍为 ACTIVE；Redis 恢复后 `refresh` 还可能重建 ACTIVE 标记，被盗令牌持续续期，撤销彻底失效（安全静默失败）。
- **建议修复方案**：DB 撤销先独立提交，Redis 写入失败落补偿表异步重放；或捕获 Redis 异常后仍提交 DB 撤销并告警。

## BUG-038｜P2 一般｜JwtDecoder 不校验 audience，服务令牌可访问用户端点

- **问题位置**：`security/SecurityConfiguration.java:79-89`（jwtDecoder）
- **问题描述**：本地 `JwtDecoder` 仅配置 `createDefaultWithIssuer`，不校验 `aud`，不区分用户令牌与服务令牌；服务令牌可通过 `anyRequest().authenticated()` 进入无 `@PreAuthorize` 的端点（如 `POST /api/v1/auth/password/change`），`Long.valueOf("service:...")` 抛 NumberFormatException → 500。
- **触发条件**：持任一合法服务令牌直接调用 User 服务 `/api/v1/**` 端点。
- **潜在影响**：纵深防御缺位；异构令牌混用导致 500 与潜在越权面扩大。
- **建议修复方案**：decoder 增加 audience 校验；转换器或端点层拒绝 `sub` 以 `service:` 开头的令牌访问用户端点。

## BUG-039｜P2 一般｜Redis 会话写入非原子，可产生永不过期的会话键

- **问题位置**：`session/RedisSessionStore.java:38-45`（writeActive）
- **问题描述**：`putAll` + `expire` 两条命令非原子；两者之间进程崩溃或连接中断时会话 hash 存在但无 TTL。
- **触发条件**：登录/刷新写入会话的瞬间进程被杀或 Redis 故障切换。
- **潜在影响**：无 TTL 的 ACTIVE 会话键永不过期，网关在线校验对该 sid 永久放行（fail-open）。
- **建议修复方案**：改用 Lua 脚本或 `SessionCallback`/MULTI 保证 HSET+EXPIRE 原子。

## BUG-040｜P2 一般｜限流与审计 IP 取 getRemoteAddr，网关后全部失真（与 BUG-071 同源）

- **问题位置**：`security/RegistrationRateLimitFilter.java:75`；各控制器审计取 IP 处（如 `AuthController.java:87`）
- **问题描述**：未解析可信代理头，服务经网关反代后远端地址恒为网关内网地址。
- **触发条件**：生产部署在网关之后（本项目既定架构）。
- **潜在影响**：全平台客户端共享同一"IP"计数桶，累计 5 次注册即触发 429，合法注册被拒（可用性事故）；登录/注册审计 IP 列全部是网关地址，失去取证价值。
- **建议修复方案**：与 BUG-071 一并修复：网关注入受控真实 IP 头，下游统一从该头读取（限定可信跳数防伪造）。

## BUG-041｜P2 一般｜注销与刷新轮转竞态，撤销可被覆写复活

- **问题位置**：`SessionRevocationService.java:70-76`（revokeFamilyByToken）；`service/RefreshSessionService.java:135-160`
- **问题描述**：注销用 `selectByTokenHash`（无 FOR UPDATE）定位 family，与刷新轮换事务不互斥：竞态窗口内刷新事务新插入的子行可能晚于撤销 UPDATE 提交而保持 ACTIVE；刷新事务的 `writeActive` 也可能晚于 `markRevoked` 把 Redis 状态从 REVOKED 覆写回 ACTIVE。
- **触发条件**：注销与同会话刷新请求并发（恶意或网络重试可制造）。
- **潜在影响**：注销失效：被撤销家族的最新 Refresh Token 仍可继续刷新，Redis 读模型复活为 ACTIVE。
- **建议修复方案**：`revokeFamilyByToken` 改用 `selectByTokenHashForUpdate` 取行锁；撤销标记用带时间戳的条件写，禁止 writeActive 覆盖 REVOKED。

## BUG-042｜P2 一般｜Outbox 分发无行锁且整批单事务（与 BUG-064 同构）

- **问题位置**：`messaging/OutboxEventDispatcher.java:46-56`
- **问题描述**：轮询 PENDING 事件用普通 `selectList`，无 `FOR UPDATE SKIP LOCKED`；整批包在单个 `@Transactional` 中，RabbitMQ 发送发生在事务内。
- **触发条件**：多副本并发轮询；或批内靠后事件异常导致事务回滚。
- **潜在影响**：同一事件被多实例重复投递；已发送成功的事件因回滚退回 PENDING 再次投递。
- **建议修复方案**：`SELECT ... FOR UPDATE SKIP LOCKED` 认领；每个事件独立短事务，发送与状态更新解耦。

## BUG-043｜P3 建议｜InternalApiFilter 空名单 NPE → 500

- **问题位置**：`security/InternalApiFilter.java:63-68`
- **问题描述**：`allowedClientIds().contains(...)` 无 null 防御；配置缺失时绑定可能为 null，所有 `/internal/v1/**` 请求抛 NPE → 500 而非 401/403。
- **触发条件**：`EDUCLOUD_USER_INTERNAL_ALLOWED_CLIENT_IDS` 未配置。
- **潜在影响**：内部接口以 500 形式暴露异常栈，非 fail-closed 语义。
- **建议修复方案**：过滤器对 `allowedClientIds` 判空视为空名单直接 403；配置类默认空列表。

## BUG-044｜P3 建议｜服务令牌签发端点缺防护与事务

- **问题位置**：`service/ServiceTokenService.java:61-86`（issue）
- **问题描述**：① `selectOne` 在数据异常多条命中时抛 `TooManyResultsException` → 500；② 端点无失败计数/限流，可对 client_secret 撞库；③ `issue` 无 `@Transactional`，读取与签发之间客户端被禁用仍可得令牌。
- **触发条件**：凭据表重复有效行；攻击者高频爆破。
- **潜在影响**：签发端点成为暴力破解放大器；禁用客户端撤销滞后最长 5 分钟。
- **建议修复方案**：`selectList` 取最高版本有效行；按 clientId 加失败锁定/限流；结合 tokenVersion 缩短容忍窗口。

## BUG-045｜P3 建议｜注册幂等事后记录，并发下语义退化

- **问题位置**：`service/IdempotencyService.java:73-80`；`controller/AuthController.java:97-103`
- **问题描述**：幂等 `record` 在 `register` 事务提交后执行；并发双请求都通过 `findReplay`，一个建号成功后另一个在 `record` 插入时抛 `IDEMPOTENCY_CONFLICT`。
- **触发条件**：同一 Idempotency-Key 并发重发注册。
- **潜在影响**：账号已创建但客户端收到 409；重试又得"用户名已占用"，幂等语义退化为不确定性失败。
- **建议修复方案**：幂等键占位插入前移到业务事务之前；或冲突时回读已存在记录按其结果应答。

## BUG-046｜P3 建议｜登录失败计数丢失更新，锁定阈值被抬高

- **问题位置**：`AuthenticationService.java:209-223`（registerFailedAttempt，已复核）
- **问题描述**：计数为"读-算-写"且无行锁/条件更新：`count = getFailedLoginCount()+1` 后无条件 `set`。
- **触发条件**：同一账号并发多次错误密码登录。
- **潜在影响**：并发下计数丢失，锁定阈值实际被抬高（配 5 次实际 8-10 次才锁），削弱暴力破解防护。
- **建议修复方案**：原子更新 `SET failed_login_count = failed_login_count + 1`，`>= max` 时同语句置锁。

## BUG-047｜P3 建议｜Refresh Token Cookie 默认不带 Secure 标志

- **问题位置**：`resources/application.yml:48`（cookie-secure 默认 false）；`AuthController.java:183-191`
- **问题描述**：Refresh Token Cookie 不强制 Secure（HttpOnly/SameSite=Lax 已具备）。
- **触发条件**：生产部署遗漏 `EDUCLOUD_USER_COOKIE_SECURE=true`。
- **潜在影响**：Refresh Token 可能经明文 HTTP 传输被中间人窃取，会话劫持。
- **建议修复方案**：代码默认值改为 true，本地开发显式关闭；生产环境检测到 false 输出启动告警。

## BUG-048｜P3 建议｜角色分配事件 aggregateVersion 不递增，下游去重失真

- **问题位置**：`service/UserAdminService.java:155-163`（assignRoles）
- **问题描述**：事件 `aggregateVersion` 取 `user.getVersion() + 1`，但方法从未更新 `sys_user.version`。
- **触发条件**：同一用户连续两次分配角色。
- **潜在影响**：两次事件携带相同 aggregateVersion，按版本去重/排序的下游投影可能丢弃或乱序处理。
- **建议修复方案**：分配时对 `sys_user.version` 原子递增并以递增后的值为 aggregateVersion。

## BUG-049｜P3 建议｜登录端点无频率限制，撞库可低速进行

- **问题位置**：`controller/AuthController.java:106-119`（login）；`security/RegistrationRateLimitFilter.java`（仅覆盖注册）
- **问题描述**：限流只覆盖注册端点；账号级锁定之外无全局/单 IP 登录节流。
- **触发条件**：攻击者对大量账号各尝试 1-4 次（规避单账号锁定阈值）。
- **潜在影响**：凭证填充攻击可长期低速进行，仅靠审计事后发现。
- **建议修复方案**：对 `/api/v1/auth/login` 增加 IP+设备 Redis 限流，或渐进式延迟/验证码升级。

## BUG-050｜P3 建议｜头像变更远程绑定无补偿，本地回滚留孤儿数据

- **问题位置**：`service/ProfileService.java:87-94`（updateProfile）
- **问题描述**：先远程 `bindAvatar` 再远程 `unbindAvatar` 之后才写本地库；本地事务回滚无法撤销已生效的远程 bind。
- **触发条件**：bind 成功后 unbind 或本地写库失败。
- **潜在影响**：File 侧残留绑定关系与本地档案不一致，长期积累孤儿授权数据。
- **建议修复方案**：失败时反向 unbind 补偿；或改为本地先落库 + 事件驱动 file 侧绑定的最终一致方案。

---

# 四、educloud-course（10 项）

## BUG-051｜P1 严重｜开课消息消费异常被吞，付款后权益永久丢失（已逐行复核）

- **问题位置**：`educloud-course/src/main/java/com/educloud/course/messaging/OrderPaidListener.java:43-46`
- **问题描述**：消费 `OrderPaidEvent` 时对 `enrollPaidCourse` 抛出的异常 `catch (Exception ex)` 仅记日志吞掉；Spring AMQP 默认 AUTO ack 下监听器正常返回即确认消息，不触发 nack/重试/死信。
- **触发条件**：开课过程中任何瞬时故障（DB 抖动、行锁等待超时、死锁被牺牲）。
- **潜在影响**：学生已付款但课程开通永久丢失且无补偿（消息已确认，队列无痕迹），资金权益类数据丢失。
- **建议修复方案**：失败时向外抛出异常让容器 nack 重投（配置重试 + 死信队列）；仅对确定不可重试的异常放行；增加按 order_id 反查 enrollment 的对账任务。

## BUG-052｜P1 严重｜审核驳回/撤回把已发布课程生命周期打回 DRAFT

- **问题位置**：`service/CourseAuditService.java:276`（reject）、`:332`（withdraw）
- **问题描述**：驳回/撤回无条件把 `course.lifecycleStatus` 置回 `DRAFT`，不恢复提交审核前的状态；已发布课程迭代新版本被驳回后，生命周期从 PUBLISHED/OFFLINE 被改写为 DRAFT，而 `published_version_id` 仍在。
- **触发条件**：已发布课程迭代：PUBLISHED/OFFLINE → createDraft → submitForReview → reject 或 withdraw。
- **潜在影响**：课程从公开目录消失（目录仅查 PUBLISHED）；进入死锁态——`offline` 仅允许 PUBLISHED、`republish` 仅允许 OFFLINE，只能重新提审通过才恢复上架，中间态自相矛盾。
- **建议修复方案**：课程根记录"提交前生命周期"，驳回/撤回时恢复原状态；或判断 `published_version_id != null` 时回落 OFFLINE/PUBLISHED。

## BUG-053｜P1 严重｜撤回清空 draftVersionId 失效（MyBatis-Plus 空值策略）

- **问题位置**：`CourseAuditService.java:335-336`（withdraw）；对照 `approve()` L213-219 注释明确指出此坑并改用 `LambdaUpdateWrapper.set(..., null)`
- **问题描述**：撤回时 `course.setDraftVersionId(null)` 后 `updateById`；MyBatis-Plus 默认 `updateStrategy=NOT_NULL`，null 字段不进 SET 子句，`draft_version_id` 清不掉，行为与 Javadoc 不符。
- **触发条件**：任何一次审核撤回操作。
- **潜在影响**：撤回后 `draft_version_id` 仍指向 WITHDRAWN 版本：`GET draft` 把 WITHDRAWN 版本当草稿下发；若其他路径正确清空指针则触发 BUG-057 的 500。
- **建议修复方案**：与 `approve` 一致用 `LambdaUpdateWrapper.set(CourseEntity::getDraftVersionId, null)` 显式置空；补集成测试断言撤回后为 NULL。

## BUG-054｜P2 一般｜付费开课不校验课程生命周期，归档课程照常开课

- **问题位置**：`service/EnrollmentService.java:159-162`（enrollPaidCourse）
- **问题描述**：付费开课路径完全不校验课程生命周期：课程不存在静默返回 null，OFFLINE/ARCHIVED 也照常插入 ACTIVE 选课；免费路径严格限定 PUBLISHED，两路径规则不一致。
- **触发条件**：支付完成时课程已下架/归档，或订单携带非法 courseId。
- **潜在影响**：学生对归档（终态）课程获得学习权限；courseId 无效时权益被静默吞掉，而监听器还打印成功日志误导排障。
- **建议修复方案**：至少拒绝 ARCHIVED 并打告警指标；课程不存在时抛异常走重试/死信，而非返回 null。

## BUG-055｜P2 一般｜付费开课选课计数不校验影响行数，乐观锁条件形同虚设

- **问题位置**：`EnrollmentService.java:180`；`mapper/CourseMapper.java:20-22`
- **问题描述**：付费路径 `incrementEnrollmentCount` 不检查返回行数（免费路径有 0 行 → 409 校验）；该 SQL 带 `AND version = #{version}` 却从不递增 version。
- **触发条件**：版本条件不匹配（当前因持锁不可达，属防御缺口）或未来改动打破前提。
- **潜在影响**：`enrollment_count` 与实际选课行数漂移，目录排序失真，无日志无指标。
- **建议修复方案**：检查返回行数并告警/抛异常；或去掉无意义的 version 条件，统一两路径校验风格。

## BUG-056｜P2 一般｜免费/付费选课加锁顺序相反，可造成 InnoDB 死锁

- **问题位置**：`EnrollmentService.java:113`（enroll）vs `:147`（enrollPaidCourse）
- **问题描述**：免费路径先锁课程根再插入 enrollment；付费路径先在 `uk(course_id,student_id)` 上 `SELECT ... FOR UPDATE`（RR 下产生间隙锁）再锁课程根。
- **触发条件**：同一课程同一学生并发触发免费选课与付费开课。
- **潜在影响**：锁顺序交叉造成死锁，一方事务回滚；付费侧回滚后叠加 BUG-051 即权益丢失。
- **建议修复方案**：统一加锁顺序（均先锁课程根，再操作 enrollment 行）。

## BUG-057｜P2 一般｜课程详情对空草稿指针 500

- **问题位置**：`service/CourseCatalogService.java:170-173`（detail）
- **问题描述**：DRAFT/PENDING_REVIEW 时 `versionId = course.getDraftVersionId()` 未判空直接 `versionMapper.selectById(versionId)`；MyBatis-Plus 对 null id 抛异常而非返回 null，走不到下方 404 分支。
- **触发条件**：草稿指针为空的 DRAFT 课程被详情访问（当前被 BUG-053 掩盖，一旦修复即触发）。
- **潜在影响**：详情接口 500；对归属教师本应返回 404。
- **建议修复方案**：`versionId == null` 时抛 `COURSE_NOT_FOUND`。

## BUG-058｜P2 一般｜Outbox 分发整批单事务跨网络 IO（与 BUG-042/064 同构）

- **问题位置**：`messaging/OutboxEventDispatcher.java:54-56`
- **问题描述**：整批 50 条"发 MQ + 标记 PUBLISHED"包在同一 `@Transactional`：前 49 条已发送，第 50 条异常则整批回滚，已发送事件退回 PENDING 再次投递；事务在每次 `convertAndSend` 期间持有 DB 连接。
- **触发条件**：批内任一事件失败；多实例并发轮询（代码注释已承认无 SKIP LOCKED）。
- **潜在影响**：重复投递放大；长事务占用连接池。
- **建议修复方案**：每条事件独立短事务；批量查询后先释放连接再投递。

## BUG-059｜P3 建议｜待审核列表 N+1 查询

- **问题位置**：`CourseAuditService.java:355-360`（listPending）
- **问题描述**：逐条 `courseMapper.selectById` + `versionMapper.selectById`，每页最多 100 条即 200 次额外查询。
- **触发条件**：管理端打开待审核分页列表。
- **潜在影响**：审核高峰响应变慢，DB QPS 放大。
- **建议修复方案**：改 JOIN 查询或 `selectBatchIds` 批量。

## BUG-060｜P3 建议｜分类树递归无环路防护，脏数据可打挂公开接口

- **问题位置**：`service/CategoryService.java:61-70`（toResponse）
- **问题描述**：递归构建分类树未做环路防护；`parent_id` 成环（A→B→A）时无限递归。
- **触发条件**：分类数据出现环状父子关系（误操作或迁移脚本缺陷）。
- **潜在影响**：`StackOverflowError` → 匿名可达的 `GET /api/v1/categories` 500。
- **建议修复方案**：递归携带已访问 id 集合，命中环即跳过并告警。

---

# 五、educloud-file（10 项）

## BUG-061｜P2 一般｜删除接口对已删除文件不幂等，重复发事件

- **问题位置**：`educloud-file/src/main/java/com/educloud/file/service/FileObjectService.java:92-117`（deleteIfUnbound）
- **问题描述**：删除前未检查 `root.getStatus()`：对已 DELETED 的文件重新置 DELETED、覆盖 deletedAt、version+1，并再次发布 FileDeleted 事件。
- **触发条件**：内部调用方对同一 fileId 重复调用删除接口。
- **潜在影响**：重复事件放大下游容错压力；`deleted_at` 被覆盖影响保留期计算。
- **建议修复方案**：判活前增加 `if (STATUS_DELETED.equals(root.getStatus())) return;` 短路；仅 AVAILABLE→DELETED 迁移时发事件。

## BUG-062｜P2 一般｜上传 Content-Type 二次校验形同虚设（无魔数校验）

- **问题位置**：`service/UploadSessionService.java:173-177`（doComplete）；`storage/MinioStorageGateway.java:50-63`
- **问题描述**：`stat.contentType()` 取自 MinIO 对象元数据，而该元数据正是上传客户端自行发送的 Content-Type 请求头（presigned PUT 未将 Content-Type 纳入签名）；无魔数/内容嗅探校验。
- **触发条件**：持有 `file:upload` 权限的用户 PUT 任意字节内容同时把请求头设为白名单内值。
- **潜在影响**：类型白名单形同虚设：恶意/不符内容以合法类型入库并被业务信任使用。
- **建议修复方案**：对图片类做魔数头校验；或至少把声明的 `session.contentType` 与 stat 结果一致性比对，不一致拒绝。

## BUG-063｜P2 一般｜上传确认长事务内包含 MinIO 全量读取，可拖垮连接池

- **问题位置**：`UploadSessionService.java:135-197`（doComplete）
- **问题描述**：`@Transactional` + `selectByIdForUpdate` 行锁后，在事务内先后执行两次 MinIO 网络调用（stat + 全量 SHA-256 流式读取，最大 10MB），数据库行锁与连接在整个远程 I/O 期间持有。
- **触发条件**：MinIO 响应缓慢或网络抖动时执行上传确认。
- **潜在影响**：连接池被长时间占用，清理任务被阻塞，并发上传确认时吞吐骤降。
- **建议修复方案**：MinIO 校验移到事务之前（先远程校验、后短事务落库）。

## BUG-064｜P2 一般｜Outbox 分发长事务 + 无行锁（与 BUG-042/058 同构）

- **问题位置**：`educloud-file/src/main/java/com/educloud/file/messaging/OutboxEventDispatcher.java:44-98`
- **问题描述**：① 整批（最多 50 条，含 MQ 网络发送）包在单一 `@Transactional`；② 事件选取无行锁/实例认领，多实例可重复投递；③ 批次尾部异常整事务回滚时，已发送消息状态丢失且 `attempt_count` 未持久化，退避机制失效。
- **触发条件**：多副本部署；或批内任一事件状态更新失败。
- **潜在影响**：重复投递放大；回滚场景下同一事件无退避地高频重投。
- **建议修复方案**：`FOR UPDATE SKIP LOCKED` 认领后逐条提交；"先标 SENDING 提交→发送→标 PUBLISHED"两段式，发送放事务外。

## BUG-065｜P3 建议｜存储探测限流 INCR/EXPIRE 非原子 + 路径匹配不一致

- **问题位置**：`security/StorageTestRateLimitFilter.java:62-81`
- **问题描述**：① `increment` 与 `expire` 两次独立调用，其间崩溃则计数 key 永久无 TTL，该用户被永久限流；② 路径匹配用 `getRequestURI()`，与同包 InternalApiFilter 的 `getServletPath()` 不一致，配置 context-path 时限流被绕过。
- **触发条件**：INCR 后、EXPIRE 前宕机；部署带 context-path。
- **潜在影响**：特定用户永久 429；或限流整体失效。
- **建议修复方案**：Lua 脚本原子化或 `TTL==-1` 时补 EXPIRE；路径判断统一用 `getServletPath()`。

## BUG-066｜P3 建议｜下载授权 expiresAt 不感知 7 天钳制且允许 0 秒 TTL

- **问题位置**：`service/DownloadGrantService.java:104、157、215-224`；`MinioStorageGateway.java:85-88`
- **问题描述**：`expiresAt = now + ttl` 未感知 `expirySeconds` 的 7 天钳制；`resolveTtl` 允许 `requestedTtl=0`，生成 `X-Amz-Expires=0` 的即时失效 URL。
- **触发条件**：`max-ttl` 配置超过 7 天；调用方传 `requestedTtlSeconds=0`。
- **潜在影响**：调用方按 `expiresAt` 缓存 URL 时提前失效；0 秒 TTL 产生无效授权。
- **建议修复方案**：`expiresAt` 采用钳制后的实际有效时长；`resolveTtl` 拒绝 `Duration.ZERO`。

## BUG-067｜P3 建议｜审计 IP 盲信 X-Forwarded-For 首段

- **问题位置**：`support/FileAccessAuditWriter.java:80-83`（clientIp）
- **问题描述**：审计 IP 直接信任 `X-Forwarded-For` 首段，未校验来源是否为可信代理。
- **触发条件**：攻击者绕过网关直达服务。
- **潜在影响**：审计记录中的客户端 IP 可伪造，削弱溯源能力。
- **建议修复方案**：仅来源为可信网关地址段时采纳 XFF（取最右可信段），否则记录 `getRemoteAddr()`。

## BUG-068｜P3 建议｜presigned PUT 无大小约束，存储可被合法令牌填充

- **问题位置**：`UploadSessionService.java:89-116`（doCreate）；`MinioStorageGateway.java:50-63`；`CreateUploadSessionRequest.java:13`
- **问题描述**：`expectedSizeBytes` 可选且允许负数（无 `@PositiveOrZero`），MinIO 侧不限制单次 PUT 大小；超限对象只能等会话过期后被清理。
- **触发条件**：持有 `file:upload` 的用户上传远超 10MB 上限的大对象且不 complete。
- **潜在影响**：存储容量被合法令牌滥用填充。
- **建议修复方案**：`expectedSizeBytes` 加 `@PositiveOrZero`；接入层配额或缩短清理周期 + 大小告警。

## BUG-069｜P3 建议｜审计写入失败阻断下载授权主流程

- **问题位置**：`DownloadGrantService.java:106`
- **问题描述**：GRANTED 路径中审计 INSERT（无事务、自动提交）与授权主流程强耦合：审计插入失败时 presigned URL 已生成但请求以 500 返回。
- **触发条件**：审计表写入瞬时失败（连接抖动、表空间满）。
- **潜在影响**：审计故障放大为业务失败；URL 已签发而调用方收到错误。
- **建议修复方案**：审计失败降级为 WARN 日志不阻断授权（或异步落审计）。

## BUG-070｜P3 建议｜内部接口过滤器仅捕获 JwtException，异常穿透为 500

- **问题位置**：`educloud-file/src/main/java/com/educloud/file/security/InternalApiFilter.java:58-63`
- **问题描述**：仅捕 `JwtException`；`jwtDecoder.decode` 抛其他 RuntimeException 时异常穿透过滤器产生 500 而非 401。
- **触发条件**：构造特殊格式的 Authorization 头。
- **潜在影响**：对外暴露 500，可被用于探测；未严格遵循"非法令牌一律 401"的失败关闭语义。
- **建议修复方案**：catch 扩大为 `RuntimeException`，记录日志后统一返回 401。

---

# 六、educloud-gateway（8 项）

## BUG-071｜P2 一般｜真实客户端 IP 未注入下游，审计与限流全部失真（与 BUG-040 同源）

- **问题位置**：`educloud-gateway/src/main/java/com/educloud/gateway/security/IdentityHeaderWebFilter.java:65-75`；`security/ClientIpResolver.java:34-65`；`ratelimit/GatewayRateLimitWebFilter.java:130`
- **问题描述**：网关剥离了转发头并解析出真实客户端 IP，但只写入 exchange 属性（随后被限流过滤器 `remove` 掉），从未以请求头注入下游；下游服务一律用 `getRemoteAddr()` 拿到网关内网地址。
- **触发条件**：任何经过网关的请求。
- **潜在影响**：登录/注册审计 IP 全部为网关内网地址，追溯失效；user 服务的 `register-ip` 限流桶被所有客户端共享，正常流量即可误触发限流。
- **建议修复方案**：剥离不可信头后，将 `clientIpResolver.resolve()` 结果以受控头（如 `X-Real-Ip`，需先纳入 BUG-077 的剥离清单）注入下游，下游统一读取；`remove` 改 `get`。

## BUG-072｜P2 一般｜直播 WebSocket 路由浏览器端无法完成鉴权，实际不可用

- **问题位置**：`resources/application.yml:75-79`（live-ws 路由）；`security/SecurityConfiguration.java:44-47`；`security/AccessPolicy.java:28-42`
- **问题描述**：`/ws/v1/live/**` 被分类为 PROTECTED，需 `Authorization: Bearer` 头；浏览器 WebSocket API 无法在握手请求中携带自定义头，网关也没有从 query 参数提取 JWT 的过滤器。
- **触发条件**：浏览器客户端发起直播 WebSocket 握手。
- **潜在影响**：所有浏览器端 WS 连接在网关层即被 401 拒绝，live-ws 路由实际不可用（鉴权设计与协议能力矛盾）。
- **建议修复方案**：先用 Bearer 换取一次性 ws-ticket，握手时校验；勿直接 permitAll。

## BUG-073｜P2 一般｜非 404 的 ResponseStatusException 被映射为 500

- **问题位置**：`error/GatewayWebExceptionHandler.java:57-82`（map 方法）
- **问题描述**：仅对 404 特判，其余状态的 `ResponseStatusException`（405、411 等）沿 cause 链遍历后落入兜底分支返回 `INTERNAL_ERROR`(500)。
- **触发条件**：请求链抛出非 404 的 `ResponseStatusException`。
- **潜在影响**：客户端收到 500 而非真实 4xx；`INTERNAL_ERROR` 以 ERROR 级别记录污染告警监控。
- **建议修复方案**：按 `getStatusCode()` 映射到对应错误码；无对应码时至少保留原状态。

## BUG-074｜P3 建议｜RouteGroups 与 application.yml 路由模式不同步，限流/指标失真

- **问题位置**：`route/RouteGroups.java:32-39` vs `resources/application.yml:49-54`
- **问题描述**：CONTENT 分组缺少 yml 中的教师草稿/审核类路径（`/api/v1/admin/content-audits/**`、`/api/v1/student/courses/**`、`/api/v1/teacher/**` 多个模式），这些路径 `forPath` 返回 UNMATCHED。
- **触发条件**：请求命中上述路径。
- **潜在影响**：不相干路径合并进同一 `unmatched` 限流桶，隔离粒度失真；指标 routeGroup 标签错误。路由转发不受影响。
- **建议修复方案**：同步补入 RouteGroups，或用测试断言两处模式集合一致。

## BUG-075｜P3 建议｜错误写入器在 requestId 缺失时自身抛异常

- **问题位置**：`error/GatewayErrorWriter.java:42`（requireRequestId）；`GatewayWebExceptionHandler.java:49`
- **问题描述**：异常处理器自身对 requestId 做了防御性降级，但随即调用的 `errorWriter.write` 内部用 `requireRequestId` 在属性缺失时同步抛 `IllegalStateException`。
- **触发条件**：REQUEST_ID 属性缺失时的错误响应写入路径（未来新增更小 order 的过滤器即可触发）。
- **潜在影响**：兜底异常处理器失效，错误响应丢失、连接被粗暴关闭且掩盖原始异常。
- **建议修复方案**：`write` 内部改用非抛异常的降级取值，缺失时生成临时 UUID。

## BUG-076｜P3 建议｜限流过滤器破坏性删除 CLIENT_IP 属性

- **问题位置**：`ratelimit/GatewayRateLimitWebFilter.java:130`
- **问题描述**：`buckets()` 用 `exchange.getAttributes().remove(...)` 读后即删该属性。
- **触发条件**：每个经过限流过滤器的请求。
- **潜在影响**：后续任何组件需要客户端 IP 时拿到 null（隐式契约破坏）。
- **建议修复方案**：改为 `get`；若需防二次消费应以注释说明。

## BUG-077｜P3 建议｜不可信头剥离清单未包含 X-Real-Ip

- **问题位置**：`security/IdentityHeaderWebFilter.java:26-33`
- **问题描述**：剥离清单覆盖 `x-forwarded-*`、`forwarded`、身份头，但未含 `X-Real-Ip`（当前无下游消费，暂无实际利用面）。
- **触发条件**：未来任一服务开始信任该头。
- **潜在影响**：潜在请求头伪造通道；一旦采用 BUG-071 的 `X-Real-Ip` 注入方案，必须先纳入剥离清单否则可被伪造。
- **建议修复方案**：将 `x-real-ip` 加入剥离清单。

## BUG-078｜P3 建议｜无 TTL 会话键被判 CORRUPT 返回 503，误导排障

- **问题位置**：`security/RedisSessionVerifier.java:86-88`；`resources/com/educloud/gateway/security/check-session.lua:2-6`
- **问题描述**：`check-session.lua` 对无 TTL 会话键返回 `PTTL=-1`，Java 侧判 `CORRUPT` 并返回 503 `DEPENDENCY_UNAVAILABLE`。
- **触发条件**：user 服务写入会话键未设过期时间（与 BUG-039 相关）。
- **潜在影响**：会话数据有效却被判腐坏返回 503，排障方向被误导到依赖可用性。
- **建议修复方案**：与 user 服务会话写入约定对齐并加契约测试；或将"无 TTL"单独建模为 401 强制重新登录。

---

# 七、educloud-common（8 项）

## BUG-079｜P1 严重｜雪花 ID 自动装配缺依赖排序，开启后静默降级为随机 worker 位（多实例主键冲突）

- **问题位置**：`educloud-common/src/main/java/com/educloud/common/config/CommonIdentifierAutoConfiguration.java:20-29`
- **问题描述**：整套 identifier 自动装配缺少 `after = RedisAutoConfiguration.class`。按类名字母序 `com.educloud...` 先于 `org.springframework...RedisAutoConfiguration` 处理，评估 `@ConditionalOnBean(StringRedisTemplate.class)` 时 Bean 尚未注册，条件判定失败，整套 worker lease 组件静默不装配；各服务降级为随机 worker 位的雪花 ID。
- **触发条件**：任一服务开启 `educloud.common.id.enabled=true`（当前尚无服务开启，属潜伏问题）。
- **潜在影响**：多实例部署时雪花 ID 主键冲突，与该模块设计目标完全相悖，且无任何报错。
- **建议修复方案**：类上改为 `@AutoConfiguration(after = {CommonCoreAutoConfiguration.class, RedisAutoConfiguration.class})`；消费方增加启动自检快速失败。

## BUG-080｜P1 严重｜租约续租失败即永久失效，Redis 抖动可致全集群写入瘫痪

- **问题位置**：`id/WorkerLeaseManager.java:209-237`（renewSafely）、`:81-83`（start 拒绝重启）、`:120-125`；`id/WorkerLeaseIdentifierGenerator.java`（sleep 中断即 `permanentlyUnavailable`）
- **问题描述**：续租失败（含 Redis 瞬时抖动的 RuntimeException）与续租被拒同等处理：一律置 `INACTIVE` 且无重新获取租约的恢复路径；`start()` 对非 NEW 状态拒绝重启；一次线程中断即永久置位失效。
- **触发条件**：一次短暂 Redis 连接闪断（续租周期 10s 内命中即触发）。
- **潜在影响**：该实例 ID 生成永久失效（所有写入链路抛 `IdentifierUnavailableException`），只能重启恢复；Redis 抖动同时影响全部实例时全集群写操作瘫痪。
- **建议修复方案**：区分"所有权丢失"与"基础设施异常"，后者在本地 deadline 内带退避重试；允许 INACTIVE 重新 `tryAcquire`；中断仅取消当前调用。

## BUG-081｜P2 一般｜watermark 滞后 + 三套时钟混用，崩溃接管场景可能产生重复雪花 ID

- **问题位置**：`resources/com/educloud/common/id/renew-worker.lua:8-12`、`release-worker.lua:5-9`；`WorkerLeaseManager.java:148-156`
- **问题描述**：watermark 只在续租/释放时写入，两次续租之间发放的时间戳不受 fence 保护；租约过期由 Redis 时钟裁决、发放时间戳由应用墙钟产生、本地失效期限由 `nanoTime` 裁决；`stop()` 读 watermark 与生成器 `recordIssuedTimestamp` 不在同一把锁上。
- **触发条件**：进程临近租约过期时崩溃（未走 stop）且崩溃前已发放过 ID，同时应用主机墙钟领先 Redis 主机（NTP 漂移）。
- **潜在影响**：新实例获取同一 worker 槽位后可能发出与已崩溃实例相同的雪花 ID，击穿唯一性保证。
- **建议修复方案**：获取脚本改为 `redisNowMillis > watermark + 安全缓冲`，或发放侧异步推进 watermark；`stop()` 确认无 in-flight 生成后再读 watermark。

## BUG-082｜P3 建议｜失效/停机早退路径不释放租约键，槽位被无效占用

- **问题位置**：`WorkerLeaseManager.java:141-147`（stop 早退）、`:268-276`（invalidateLocally）
- **问题描述**：本地失效或 `stop()` 状态非 ACTIVE 时不尝试向 Redis 释放仍被本实例持有的租约键，只能等 `PX` 过期。
- **触发条件**：实例在 INACTIVE 状态被容器关闭。
- **潜在影响**：32 个 worker 槽位在最长一个 `leaseTtl`（30s）内被无效占用；大规模滚动重启时可能耗尽槽位。
- **建议修复方案**：早退路径中按当前 `workerId`/`ownerId` 尽力调用 `release`（失败仅告警）。

## BUG-083｜P3 建议｜RequestContextFilter 注册顺序过晚，早期日志缺 requestId

- **问题位置**：`config/CommonServletWebAutoConfiguration.java:41-45`
- **问题描述**：作为普通 Filter Bean 注册，默认 `LOWEST_PRECEDENCE`，排在 `springSecurityFilterChain`（order -100）之后。
- **触发条件**：早期过滤器链输出日志。
- **潜在影响**：早期日志缺 `requestId` MDC，响应头 `X-Request-Id` 延迟设置，链路关联不完整。
- **建议修复方案**：改用 `FilterRegistrationBean` 设高优先级（如 `HIGHEST_PRECEDENCE + 10`）。

## BUG-084｜P3 建议｜超长 request id 截断为 36 字符，不同请求可塌缩为同一 ID

- **问题位置**：`web/RequestIdPolicy.java:26-28`
- **问题描述**：合法但超过 36 字符的客户端 request id 被 `substring(0, 36)` 截断，前缀相同的不同客户端 ID 塌缩为同一 request_id。
- **触发条件**：客户端使用长格式追踪 ID（64 位 hex、traceparent 风格）且存在相同前缀。
- **潜在影响**：审计表（`request_id VARCHAR(36)`）中不同请求被误关联。
- **建议修复方案**：超长值直接视为非法改由服务端生成，保持"要么原样落库、要么服务端生成"的二元语义。

## BUG-085｜P3 建议｜异步线程每次调用生成新 requestId，破坏关联语义

- **问题位置**：`web/ServletRequestContextAccessor.java:21-29`
- **问题描述**：非 Servlet 请求线程（异步任务、定时任务、消息消费者）调用时，每次都 `resolve(null)` 生成全新 UUID。
- **触发条件**：异步/后台线程中多次调用 `ApiResponseFactory` 或写多条审计/事件。
- **潜在影响**：同一逻辑操作的响应体、日志、事件得到互不相同的 requestId。
- **建议修复方案**：异步场景由调用方显式传递（如 `EventEnvelope.requestId`），或按"一次逻辑调用一个 ID"缓存。

## BUG-086｜P3 建议｜ID 租约 Lua 脚本的健壮性隐患（旧版 Redis/脏数据）

- **问题位置**：`resources/com/educloud/common/id/acquire-worker.lua:1-2、:10`（renew/release 同理）
- **问题描述**：① 脚本开头使用非确定性命令 `redis.call('TIME')`，在 Redis 3.x/4.x 上未调 `redis.replicate_commands()` 会报错；② `tonumber(GET watermarkKey or '0')` 若键被写坏为非数字，`tonumber` 返回 nil 触发 Lua 运行期错误，所有槽位获取失败。
- **触发条件**：降级使用旧版 Redis；或 watermark 键被误写。
- **潜在影响**：ID 服务整体不可用（fail-closed，但故障面放大）。
- **建议修复方案**：锁定 Redis ≥ 5.0 并加兼容层 `if redis.replicate_commands then ... end`；watermark 解析改 `tonumber(...) or 0` 防御写法。

---

# 八、已确认的正面情况（避免误报声明）

以下重点区域经逐行审查未发现问题，列出以证明审查覆盖面：

1. **网关 `/internal/**` 三层防护**：全部 17 条路由无指向 `/internal/**`、`discovery.locator.enabled=false`、`InternalPathWebFilter` 返回 404、Security 链 `denyAll()`，未发现公网可达内部接口的路径。
2. **网关身份头伪造防护**：`X-User-Id`/`X-Role(s)` 等身份头均被剥离（含大小写归一化），网关不向下游注入身份头，无伪造透传链。
3. **JWT 签名安全**：网关/订单/内容服务均仅接受 RS256 + kid 白名单，JWKS 加载拒绝私钥参数，无 alg=none/HMAC 混淆路径。
4. **订单状态机 CAS**：支付/关单/取消三路均以 `WHERE status = fromStatus` 条件更新，"支付+关单"并发仅一路成功；延时关单消费幂等；幂等 Lua 脚本原子正确；学生越权访问他人订单在详情/取消/支付三处均有归属校验。
5. **用户服务密码与会话**：BCrypt 用法正确（72 字节收口、不存在账号 dummyHash 时序对齐）；Refresh Token 只存 SHA-256；轮换使用 FOR UPDATE 行锁 + 条件更新；JWKS 仅输出公钥。
6. **文件服务流资源**：`MinioStorageGateway` 下载/SHA-256 均 try-with-resources 正确关闭，未发现流泄漏；对象键由服务端生成（UUID），无路径穿越面。
7. **课程服务金额与幂等**：金额全链路统一 `BigDecimal`，免费判定用 `compareTo(ZERO)`；`OrderPaid` 重复消费由唯一键 + FOR UPDATE 兜底，不会重复开课。
8. **公共组件雪花生成器**：`nextId()` 整体 synchronized，序列耗尽/时钟回拨/位宽边界处理均正确，未发现重复 ID 路径；MDC 在 finally 中正确恢复无 ThreadLocal 泄漏。
9. **SQL 注入**：全部模块 Mapper 自定义 SQL 均为参数绑定，未发现字符串拼接注入点。
10. **雪花 ID 字符串化规范**：抽查订单/课程/内容模块，未发现将 64 位 ID 以 Number 形式暴露给前端的违规。

# 九、审查边界声明（未覆盖部分）

- 各模块 `src/test` 测试代码未纳入本次审查对象；
- `deploy/sql` 迁移脚本中的唯一索引存在性未逐条核验（相关建议项基于代码缺失防护推断）；
- Nacos 配置中心的线上实际配置值（如网关 `allowed-origins`、可信代理网段、限流参数）不在仓库内，需另行核对；
- 前端三端已有单独的 Review 交接文档（`docs/2026-08-24-educloud-review-bug-fix-handoff.md`），不在本报告范围内；
- 报告中的行号基于 2026-08-24 工作区代码快照；未验证项均已标注依据。


# SQL AI 智批改软件 — 全栈技术方案与架构文档

---

## 一、需求深度复述与分析

### 1.1 功能模块分析

| 功能模块 | 需求描述 | 技术要求 | 优先级 |
|---------|---------|---------|--------|
| SQL编辑器 | 语法高亮、代码折叠、自动补全、查找替换 | 需要集成工业级代码编辑器组件，支持LSP-like功能 | P0 |
| 智能语法检查 | 识别关键字拼写、括号匹配、数据类型不一致等 | 需要SQL解析器+AST分析+AI增强 | P0 |
| 逻辑漏洞分析 | 关联表错误、条件判断失误、数据过滤不严谨 | 需要AI深度推理+元数据辅助 | P0 |
| 性能优化建议 | 索引优化、子查询改写、执行计划分析 | 需要EXPLAIN模拟+AI改写能力 | P0 |
| 多数据库适配 | MySQL/Oracle/SQL Server/PostgreSQL | 需要多方言解析器+方言转换能力 | P0 |
| 学习辅助 | 知识点讲解、官方文档链接、示例代码 | 需要AI生成学习材料+知识库检索 | P1 |
| 批量批改 | 多文件批量处理、报告生成 | 需要任务队列+并发控制 | P1 |
| 自定义规则 | 规则分类管理、优先级设置、表达式自定义 | 需要规则引擎+AST匹配模式 | P1 |
| 数据库元数据采集 | 连接数据库、加载表结构/字段信息 | 需要多数据库驱动+安全连接 | P0 |
| 历史记录 | 查看历史批改成绩、结果回溯 | 需要本地持久化存储 | P1 |
| 主题/外观设置 | 浅色/深色/自定义主题 | 需要CSS变量+主题引擎 | P2 |
| 报告生成导出 | 批改报告PDF/HTML导出 | 需要模板引擎+PDF生成 | P1 |
| 自动更新 | 自动检查更新、下载安装 | 需要更新服务器+增量更新 | P2 |

### 1.2 非功能需求分析

| 需求维度 | 具体要求 | 技术挑战 |
|---------|---------|---------|
| 跨平台 | Windows 7+/macOS 10.10+/Linux X11 | 原生模块的跨平台编译、不同OS的UI一致性 |
| 性能 | 大型SQL文件(>1000行)批改不卡顿 | 需使用Web Worker避免阻塞UI线程 |
| 安全性 | 数据库连接信息加密存储、数据加密传输 | 需使用OS原生密钥链+加密算法 |
| 可维护性 | 模块化架构、清晰代码结构 | 需要严格的模块边界和IPC协议设计 |
| 离线能力 | 部分批改功能可在无网络时使用 | 需要本地规则引擎+可选本地AI模型 |
| 安装包大小 | 需内置运行环境 | 需优化Electron打包，排除不必要的node_modules |

### 1.3 隐含技术挑战

| 挑战 | 描述 | 影响范围 |
|-----|------|---------|
| 多方言SQL解析 | 需要同时支持4种数据库方言的解析和校验 | 解析器选型、规则引擎设计 |
| AI Prompt工程 | 需要设计高质量、结构化的Prompt以获取稳定的JSON格式输出 | AI模块、结果解析 |
| 实时性与异步 | 编辑器实时检查(防抖<500ms)和AI批改(异步长耗时)的协调 | 编辑器、批改调度 |
| 元数据同步 | 数据库元数据可能变化，需要缓存和刷新策略 | 数据库服务、元数据缓存 |
| 并发批改 | 批量批改时的并发控制和资源管理 | 任务队列、进度通知 |
| 安全存储 | 数据库密码等敏感信息的安全存储 | 加密服务、IPC安全 |

### 1.4 本软件与Web应用/普通桌面工具的本质区别

| 对比维度 | 本软件 | Web SQL工具 | 普通桌面SQL工具 |
|---------|--------|-----------|---------------|
| 数据库直连 | 原生驱动TCP直连，无中间层 | 需服务端代理 | 原生驱动直连 |
| AI集成深度 | Prompt工程+Agent工作流 | API简单调用 | 通常无AI或浅层集成 |
| 离线能力 | 本地规则引擎+可选本地模型 | 完全依赖服务端 | 有限离线功能 |
| 安全模型 | OS原生密钥链+本地加密存储 | 服务端存储 | 明文配置 |
| 编辑器体验 | Monaco Editor(LSP-like) | CodeMirror/Ace | 基础编辑器 |
| 批量处理 | 本地并发队列+文件系统直读 | 需上传下载 | 文件系统直读 |

---

## 二、终极技术栈选型（带具体版本及决策矩阵）

### 2.1 桌面壳与跨平台方案

| 维度 | 详情 |
|------|------|
| **我们的选择** | **Electron 31.0.0** |
| 备选方案 | Tauri 2.0、Flutter Desktop 3.x |
| 决定性理由 | 1) Monaco Editor在Electron中有官方集成支持，无需额外适配层；2) Node.js原生数据库驱动(mysql2/pg等)可直接在主进程运行；3) 生态成熟度最高，electron-builder打包方案经数十万应用验证；4) 团队技能匹配度最高(JS/TS全栈) |
| 关键应用点 | BrowserWindow管理、原生菜单、系统托盘、文件对话框、安全IPC桥梁 |

### 2.2 前端UI框架

| 维度 | 详情 |
|------|------|
| **我们的选择** | **React 18.3.1 + TypeScript 5.5** |
| 备选方案 | Vue 3 + Composition API、Svelte 5 |
| 决定性理由 | 1) Monaco Editor的React封装(@monaco-editor/react)维护最活跃；2) Zustand的状态管理范式与React Hooks天然契合；3) Ant Design 5.x提供完整的IDE级组件库；4) React的并发特性(useTransition)可优化大数据量结果列表渲染 |
| 关键应用点 | 主布局(可拖拽面板)、编辑器容器、结果展示、设置面板、历史列表 |

### 2.3 核心代码编辑器组件

| 维度 | 详情 |
|------|------|
| **我们的选择** | **Monaco Editor 0.50.0 (VS Code内核)** |
| 备选方案 | CodeMirror 6、Ace Editor |
| 决定性理由 | 1) 原生支持SQL语法高亮(内置sql语言定义)；2) 可通过monaco.languages.registerCompletionItemProvider实现自定义SQL自动补全；3) 可通过setModelMarkers实现错误波浪线标记，与批改结果双向联动；4) 支持代码折叠、多光标、 minimap 等IDE级功能；5) 通过@monaco-editor/react的loader CDN分离，减小包体积 |
| 关键应用点 | SQL编辑区、diff对比视图、错误标记联动、自定义Token颜色 |

### 2.4 AI大模型接入与工程化

| 维度 | 详情 |
|------|------|
| **我们的选择** | **DeepSeek API + LangChain 0.3.x (@langchain/core) + OpenAI SDK 4.x（兼容调用）** |
| 备选方案 | 直接使用OpenAI SDK、Anthropic SDK、Vercel AI SDK |
| 决定性理由 | 1) DeepSeek API 完全兼容 OpenAI SDK（仅需改 baseURL），零迁移成本；2) 成本仅为 OpenAI 的 1/15~1/35（输入 ¥1/1M tokens，输出 ¥2/1M tokens）；3) 中文理解能力优于 GPT-4o，SQL批改场景（中文教学/错误提示）更精准；4) 国内直连无需代理，避免网络延迟问题；5) LangChain提供ChatPromptTemplate结构化Prompt管理 + StructuredOutputParser确保JSON schema固定输出；6) 架构设计支持未来一键切换其他兼容 OpenAI SDK 的模型 |
| 关键应用点 | 语法检查Prompt链、逻辑分析Chain、性能建议Chain、学习材料生成Agent |

### 2.5 多方言SQL解析与静态分析

| 维度 | 详情 |
|------|------|
| **我们的选择** | **node-sql-parser 5.3.0 + sql-formatter 15.x + antlr4 4.13.2 (自定义方言扩展)** |
| 备选方案 | jsqlparser、自研基于PEG.js的解析器 |
| 决定性理由 | 1) node-sql-parser已内置MySQL/PostgreSQL/TransactSQL/Hive四种方言AST解析；2) 解析后的AST可直接用于静态规则检查(无需AI)；3) sql-formatter提供代码标准化，便于diff对比；4) antlr4作为兜底方案处理特殊方言语法，可加载官方SQL语法文件(.g4) |
| 关键应用点 | 静态语法检查、表名/字段名提取、SQL格式化、AST级别的规则匹配 |

### 2.6 数据库连接与元数据采集

| 维度 | 详情 |
|------|------|
| **我们的选择** | **mysql2 3.11.0 + pg 8.13.0 + mssql 11.0.0 + oracledb 6.5.0** |
| 备选方案 | knex.js、sequelize、typeorm |
| 决定性理由 | 1) 原生驱动提供最完整的元数据查询能力(INFORMATION_SCHEMA、pg_catalog等)；2) 无需ORM抽象层，减少性能开销和方言差异遮蔽；3) 每个驱动独立管理，可按需安装避免包体积膨胀；4) 连接池由各驱动原生支持 |
| 关键应用点 | 表结构采集、字段类型映射、索引信息获取、执行计划EXPLAIN |

### 2.7 进程间通信(IPC)与服务架构

| 维度 | 详情 |
|------|------|
| **我们的选择** | **Electron contextBridge + ipcRenderer/ipcMain（通道命名规范: 模块:动作）** |
| 备选方案 | electron-ipc-stream、WebSocket(本地)、gRPC |
| 决定性理由 | 1) Electron原生IPC机制，零额外依赖；2) contextBridge确保渲染进程只能访问白名单API，符合安全最佳实践；3) 通道命名规范化的invoke/handle模式支持双向通信 |
| 关键应用点 | sql:parse, sql:ai-analyze, db:connect, db:fetch-metadata, batch:start, report:generate |

### 2.8 状态管理与数据持久化

| 维度 | 详情 |
|------|------|
| **我们的选择** | **Zustand 5.0 + better-sqlite3 11.0 (主进程) + electron-store 10.0 (配置)** |
| 备选方案 | Pinia(通过ipc)、Redux Toolkit + lowdb |
| 决定性理由 | 1) Zustand体积仅2KB，无需Provider包裹；2) 通过Zustand的subscribe+IPC实现主进程状态同步；3) better-sqlite3是同步API，在主进程中不会阻塞UI；4) electron-store基于JSON文件，适合窗口大小/主题等简单配置 |
| 关键应用点 | 编辑器状态、批改结果、历史记录SQLite表、用户设置JSON |

### 2.9 批量任务与队列管理

| 维度 | 详情 |
|------|------|
| **我们的选择** | **自研基于EventEmitter的任务队列 + P-Queue 9.0（并发控制）** |
| 备选方案 | Bull + Redis、Bee-Queue |
| 决定性理由 | 1) 桌面应用无需分布式队列，Bull依赖Redis会导致额外的环境依赖；2) P-Queue提供并发控制(concurrency:3)+优先级；3) 自研队列可通过IPC向渲染进程推送进度，与UI深度集成 |
| 关键应用点 | 批量批改任务调度、并发数限制(默认3)、取消/暂停/恢复 |

### 2.10 报告生成与导出

| 维度 | 详情 |
|------|------|
| **我们的选择** | **Puppeteer 23.x (HTML→PDF) + Handlebars 4.7 (HTML模板)** |
| 备选方案 | jsPDF、PDFKit、docx-templater |
| 决定性理由 | 1) Puppeteer可直接渲染React组件生成的HTML报告，样式还原度100%；2) Handlebars模板引擎在Node.js主进程中运行，不依赖DOM；3) 可复用前端CSS样式，报告与界面视觉一致 |
| 关键应用点 | 单文件报告/批量汇总报告PDF导出 |

### 2.11 自动更新与分发

| 维度 | 详情 |
|------|------|
| **我们的选择** | **electron-updater 6.3.0 (基于electron-builder)** |
| 备选方案 | Squirrel.Windows(Windows专用)、自定义更新服务 |
| 决定性理由 | 1) 跨平台统一API，一个方案覆盖Win/macOS/Linux；2) 支持增量更新(块级差异)、强制更新、灰度发布；3) 与electron-builder深度集成，发布流程无缝衔接 |
| 关键应用点 | 应用启动时检查更新、下载进度通知、静默安装 |

### 2.12 测试与代码质量保证

| 维度 | 详情 |
|------|------|
| **我们的选择** | **Playwright 1.47 (E2E) + Vitest 2.1 (单元/集成) + Electron-vite(testing集成)** |
| 备选方案 | Spectron(已废弃)、Jest、Cypress |
| 决定性理由 | 1) Playwright原生支持Electron应用测试(通过_electron.launch())；2) Vitest与Vite生态无缝集成，HMR级测试速度；3) 一套工具覆盖单元测试(parser/rule)和E2E测试(UI交互) |
| 关键应用点 | SQL解析器单元测试、IPC服务集成测试、编辑器交互E2E测试 |

---

## 三、高保真系统架构与模块详设

### 3.1 架构全景图（进程模型）

```
┌─────────────────────────────────────────────────────────────────┐
│                      MAIN PROCESS (Electron)                     │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌──────────────────┐  │
│  │ Window   │ │ Menu     │ │ Auto-     │ │ System Tray      │  │
│  │ Manager  │ │ Manager  │ │ Updater   │ │ Manager          │  │
│  └──────────┘ └──────────┘ └───────────┘ └──────────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌──────────────────┐  │
│  │ SQL      │ │ DB       │ │ AI        │ │ Report           │  │
│  │ Parser   │ │ Connector│ │ Service   │ │ Generator        │  │
│  │ Service  │ │ Service  │ │ (LangChain│ │ (Puppeteer)      │  │
│  │          │ │          │ │ +DeepSeek)│ │                  │  │
│  └──────────┘ └──────────┘ └───────────┘ └──────────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ ┌──────────────────┐  │
│  │ Batch    │ │ Rule     │ │ Encryption│ │ Storage          │  │
│  │ Queue    │ │ Engine   │ │ Service   │ │ (better-sqlite3) │  │
│  └──────────┘ └──────────┘ └───────────┘ └──────────────────┘  │
│                                                                 │
│  ====================== contextBridge =========================  │
│  IPC Channels:                                                   │
│  sql:parse | sql:ai-analyze | sql:format                         │
│  db:connect | db:disconnect | db:fetch-metadata                  │
│  batch:start | batch:cancel | batch:progress                     │
│  report:generate | report:export                                 │
│  rule:list | rule:save | rule:delete                             │
│  storage:history:list | storage:history:save                     │
│  config:get | config:set                                         │
│  file:open | file:save | file:save-as                             │
└─────────────────────────────────────────────────────────────────┘
         │                              │
         ▼                              ▼
┌─────────────────┐          ┌─────────────────────┐
│ RENDERER PROCESS│          │   AI WORKER THREAD  │
│ (React + Monaco)│          │  (可选,本地推理)     │
│                 │          │  - Ollama Client    │
│ - 编辑器页面    │          │  - Token计数        │
│ - 结果展示页面  │          │  - 模型管理         │
│ - 历史记录页面  │          └─────────────────────┘
│ - 设置页面      │
│ - 批量批改页面  │
└─────────────────┘
```

### 3.2 全部功能模块深度分解（总计18个核心模块）

---

#### 模块 01: 主进程入口与窗口管理

| 属性 | 内容 |
|------|------|
| **模块ID** | M01-MainProcess |
| **物理文件路径** | `src/main/index.ts` |
| **核心职责** | Electron主进程启动、BrowserWindow创建、应用生命周期管理 |
| **对外API** | `createMainWindow(): BrowserWindow`, `createSettingsWindow(): BrowserWindow` |
| **内部技术** | Electron 31.0.0, 窗口状态记忆(尺寸/位置), 单实例锁(app.requestSingleInstanceLock) |
| **交互流程** | 应用启动→注册IPC Handlers→创建主窗口→加载React应用→注册菜单→监听更新事件 |

**🔧 核心技术栈**:
- `electron` 31.5.0 — 主进程窗口管理、生命周期
- `electron-vite` 2.3.0 — 开发/构建工具链
- `@electron-toolkit/preload` 3.0.1 — Preload脚本工具

**🎯 推荐Skills**: `senior-fullstack`（通用架构）、`python-pro`（构建脚本）

---

#### 模块 02: IPC桥接层

| 属性 | 内容 |
|------|------|
| **模块ID** | M02-IPCBridge |
| **物理文件路径** | `src/main/ipc/index.ts`, `src/preload/index.ts` |
| **核心职责** | 定义所有IPC通道、contextBridge安全暴露、请求/响应类型约束 |
| **对外API** | 18个IPC通道，分为6个命名空间(sql/db/batch/report/rule/storage/config/file) |
| **内部技术** | ipcMain.handle, contextBridge.exposeInMainWorld, TypeScript泛型约束 |
| **交互流程** | 渲染进程调用window.api.xxx→preload暴露方法→ipcRenderer.invoke→主进程handler→返回结果 |

**🔧 核心技术栈**:
- `electron` 31.5.0 — `ipcMain.handle` / `ipcRenderer.invoke`
- `contextBridge` — 安全API暴露
- TypeScript 5.5 — 泛型约束IPC载荷类型

**🎯 推荐Skills**: `api-security-best-practices`（安全IPC设计）、`api-patterns`（通道命名规范）

**IPC通道定义**:
```typescript
// Channel命名规范: 命名空间:动作
type IPCChannels = {
  'sql:parse':       { args: [sql: string, dialect: Dialect], result: ParseResult }
  'sql:ai-analyze':  { args: [context: AnalyzeContext], result: AnalyzeResult }
  'sql:format':      { args: [sql: string, dialect: Dialect], result: string }
  'db:connect':      { args: [config: DBConnectionConfig], result: ConnectionTest }
  'db:disconnect':   { args: [connectionId: string], result: void }
  'db:fetch-metadata':{ args: [connectionId: string], result: DBMetadata }
  'batch:start':     { args: [files: string[], options: BatchOptions], result: string }
  'batch:cancel':    { args: [batchId: string], result: void }
  'batch:progress':  { args: [], result: BatchProgress } // 主→渲染: 推送事件
  'report:generate': { args: [resultIds: string[], format: 'pdf'|'html'], result: string }
  'rule:list':       { args: [category?: string], result: CustomRule[] }
  'rule:save':       { args: [rule: CustomRule], result: string }
  'rule:delete':     { args: [ruleId: string], result: void }
  'storage:history:list': { args: [filter: HistoryFilter], result: HistoryRecord[] }
  'storage:history:save': { args: [record: HistoryRecord], result: string }
  'config:get':      { args: [key: string], result: unknown }
  'config:set':      { args: [key: string, value: unknown], result: void }
  'file:open':       { args: [filters?: FileFilter[]], result: FileResult | null }
  'file:save':       { args: [content: string, defaultPath?: string], result: string | null }
}
```

---

#### 模块 03: SQL解析服务

| 属性 | 内容 |
|------|------|
| **模块ID** | M03-SQLParserService |
| **物理文件路径** | `src/main/services/sql-parser.service.ts` |
| **核心职责** | 多方言SQL解析、AST生成、静态语法验证、表/字段名提取 |
| **对外API** | `parse(sql: string, dialect: Dialect): ParseResult` |
| **内部技术** | node-sql-parser 5.3.0, antlr4 4.13.2(备用), PEG语法扩展 |
| **交互流程** | IPC接收sql:parse请求→按dialect选择解析器→生成AST→执行静态规则→返回错误列表 |

**🔧 核心技术栈**:
- `node-sql-parser` 5.3.0 — 多方言SQL解析
- `antlr4` 4.13.2 — 自定义方言扩展
- `sql-formatter` 15.4.0 — 代码标准化
- TypeScript 5.5 — AST类型约束

**🎯 推荐Skills**: `python-pro`（解析器算法参考）、`systematic-debugging`（SQL解析错误排查）

**关键类型定义**:
```typescript
type Dialect = 'mysql' | 'postgresql' | 'transactsql' | 'oracle' | 'plsql';

interface ParseResult {
  ast: ASTNode | null;
  errors: SyntaxError[];
  tables: TableReference[];      // 引用的表名
  columns: ColumnReference[];    // 引用的字段名
  statements: SQLStatement[];    // 语句类型列表
}

interface SyntaxError {
  line: number;
  column: number;
  message: string;
  severity: 'error' | 'warning';
  suggestion?: string;
}
```

---

#### 模块 04: AI批改服务

| 属性 | 内容 |
|------|------|
| **模块ID** | M04-AIAnalyzeService |
| **物理文件路径** | `src/main/services/ai-analyze.service.ts` |
| **核心职责** | 调用AI模型进行逻辑分析、性能建议、学习材料生成 |
| **对外API** | `analyze(context: AnalyzeContext): Promise<AnalyzeResult>` |
| **内部技术** | LangChain 0.3.x, OpenAI SDK 4.x（兼容DeepSeek API）, StructuredOutputParser, PromptTemplate |
| **交互流程** | 接收sql:ai-analyze→构建AnalyzeContext(Prompt+元数据)→调用DeepSeek LLM→解析JSON→返回结构化结果 |

**🔧 核心技术栈**:
- `@langchain/core` 0.3.0 — ChatPromptTemplate、StructuredOutputParser
- `@langchain/openai` 0.3.0 — 通过OpenAI兼容接口接入DeepSeek（仅改baseURL）
- `openai` 4.68.0 — 直接API调用（备用，同样兼容DeepSeek）
- `zod` 3.23.8 — JSON Schema验证AI输出

**🎯 推荐Skills**: `senior-fullstack`（AI工程化）、`claude-api`（API调用最佳实践）、`api-patterns`（Prompt链设计模式）

**DeepSeek API 接入要点**:
- baseURL: `https://api.deepseek.com`（完全兼容 OpenAI SDK）
- 模型: `deepseek-v4-flash`（对应 GPT-4o 同等能力，中文更强）

**Prompt链设计**:
1. **语法检查Prompt**: `你是专业SQL语法检查专家，全面检查{dialect}类型SQL语句，精准排查关键字拼写、表名字段名规范、括号引号逗号配对、子查询与JOIN语法结构、数据库方言专属语法规范、函数调用格式等各类语法问题，逐一定位错误位置、说明错误原因并给出标准修正写法`
2. **逻辑分析Prompt**: `专业深度解析以下SQL业务逻辑，全面检测多表JOIN关联字段不匹配、关联类型误用、WHERE/HAVING条件缺失冲突、边界值遗漏、GROUP BY与聚合字段不兼容、子查询逻辑嵌套漏洞、数据统计口径偏差、业务过滤规则失效等隐性逻辑问题，逐条说明风险点并提供规范修正思路`
3. **性能建议Prompt**: `作为{dialect}数据库专项性能优化专家，深度剖析SQL执行计划，排查全表扫描、索引缺失、索引失效、联表驱动顺序不合理、ORDER BY/GROUP BY/DISTINCT高开销、过滤条件后置、冗余嵌套子查询、临时表开销过大等性能瓶颈，从索引创建、语句改写、查询重构、参数优化给出可直接落地的专业调优方案`
4. **学习材料Prompt**: `针对以下SQL相关疑难问题，生成系统精简知识点讲解、高频易错点汇总、标准语法使用规范、实战避坑技巧，附上对应{dialect}官方权威文档直达链接，搭配标准正确示例、常见错误示例及极简可复用实战代码片段，适合入门巩固与进阶查阅`

---

#### 模块 05: 数据库连接服务

| 属性 | 内容 |
|------|------|
| **模块ID** | M05-DBConnectionService |
| **物理文件路径** | `src/main/services/db-connection.service.ts` |
| **核心职责** | 管理多数据库连接池、元数据采集、连接测试 |
| **对外API** | `connect(config: DBConnectionConfig): Promise<ConnectionResult>`, `fetchMetadata(connId: string): Promise<DBMetadata>` |
| **内部技术** | mysql2 3.11.0, pg 8.13.0, mssql 11.0.0, oracledb 6.5.0, 连接池管理 |
| **交互流程** | 接收db:connect请求→验证连接信息→创建连接池→执行元数据查询→缓存结果→返回表结构 |

**🔧 核心技术栈**:
- `mysql2` 3.11.0 — MySQL连接与元数据采集
- `pg` 8.13.0 — PostgreSQL连接与pg_catalog查询
- `mssql` 11.0.0 — SQL Server连接
- `oracledb` 6.5.0 — Oracle连接（需Python编译）

**🎯 推荐Skills**: `python-pro`（Oracle驱动编译脚本）、`api-security-best-practices`（连接信息加密存储）

---

#### 模块 06: 加密服务

| 属性 | 内容 |
|------|------|
| **模块ID** | M06-EncryptionService |
| **物理文件路径** | `src/main/services/encryption.service.ts` |
| **核心职责** | 数据库密码加密存储、AI API Key安全存储、敏感数据加解密 |
| **对外API** | `encrypt(plaintext: string): string`, `decrypt(ciphertext: string): string` |
| **内部技术** | electron safeStorage (OS原生密钥链), crypto-js AES-256-GCM(备用) |
| **交互流程** | 存储敏感配置→调用encrypt→写入SQLite/electron-store→读取时调用decrypt |

**🔧 核心技术栈**:
- `electron` 31.5.0 — `safeStorage` OS原生密钥链
- `crypto-js` 4.2.0 — AES-256-GCM加密（备用方案）

**🎯 推荐Skills**: `api-security-best-practices`（加密算法选择与密钥管理）

---

#### 模块 07: 规则引擎

| 属性 | 内容 |
|------|------|
| **模块ID** | M07-RuleEngine |
| **物理文件路径** | `src/main/services/rule-engine.service.ts` |
| **核心职责** | 管理内置规则+自定义规则、规则匹配执行、优先级排序 |
| **对外API** | `evaluate(ast: ASTNode, rules: Rule[]): RuleMatch[]` |
| **内部技术** | AST模式匹配、正则表达式、表达式解析器(自研简化版) |
| **交互流程** | 解析完成→加载启用规则→按优先级排序→逐条对AST评估→返回匹配结果列表 |

**🔧 核心技术栈**:
- `node-sql-parser` 5.3.0 — AST节点遍历与模式匹配
- 自研表达式解析器 — 规则条件表达式求值
- 正则表达式 — 文本级规则匹配

**🎯 推荐Skills**: `systematic-debugging`（规则匹配调试）、`concise-planning`（规则优先级排序算法）

---

#### 模块 08: 批量任务队列

| 属性 | 内容 |
|------|------|
| **模块ID** | M08-BatchQueue |
| **物理文件路径** | `src/main/services/batch-queue.service.ts` |
| **核心职责** | 批量批改任务调度、并发控制、进度通知、取消/暂停 |
| **对外API** | `enqueue(tasks: BatchTask[]): string`, `cancel(batchId: string): void`, `getProgress(batchId: string): BatchProgress` |
| **内部技术** | P-Queue 9.0(并发控制), EventEmitter(进度推送), 自定义任务状态机 |
| **交互流程** | 接收batch:start→创建任务列表→入队→P-Queue并发执行→每完成一个推进度→全部完成生成汇总 |

**🔧 核心技术栈**:
- `p-queue` 9.0.0 — 并发控制（默认concurrency: 3）
- `EventEmitter` — 进度事件推送
- 自定义任务状态机 — pending→running→completed/failed/cancelled

**🎯 推荐Skills**: `senior-fullstack`（任务队列架构）、`systematic-debugging`（并发Bug排查）

---

#### 模块 09: 报告生成服务

| 属性 | 内容 |
|------|------|
| **模块ID** | M09-ReportGenerator |
| **物理文件路径** | `src/main/services/report-generator.service.ts` |
| **核心职责** | 基于批改结果生成HTML报告并导出PDF |
| **对外API** | `generate(resultIds: string[], format: 'pdf'|'html'): Promise<string>` |
| **内部技术** | Handlebars 4.7(模板引擎), Puppeteer 23.x(HTML→PDF), 自定义报告CSS |
| **交互流程** | 接收report:generate→查询历史记录→渲染Handlebars模板→可选Puppeteer转PDF→返回文件路径 |

**🔧 核心技术栈**:
- `handlebars` 4.7.8 — HTML模板引擎
- `puppeteer-core` 23.5.0 — HTML→PDF转换（无头Chromium）
- 自定义报告CSS — 与前端主题一致的样式

**🎯 推荐Skills**: `senior-fullstack`（PDF生成优化）、`tailwind-patterns`（报告样式设计）

---

#### 模块 10: 本地存储服务

| 属性 | 内容 |
|------|------|
| **模块ID** | M10-StorageService |
| **物理文件路径** | `src/main/services/storage.service.ts` |
| **核心职责** | SQLite数据库初始化、CRUD操作、历史记录管理、规则持久化 |
| **对外API** | `initDatabase(): void`, `query<T>(sql: string, params?: any[]): T[]`, `execute(sql: string, params?: any[]): void` |
| **内部技术** | better-sqlite3 11.0, WAL模式, 数据库迁移脚本 |
| **交互流程** | 应用启动→initDatabase→创建表(history/rules/settings)→各服务通过storage进行持久化 |

**🔧 核心技术栈**:
- `better-sqlite3` 11.0 — 同步SQLite API（WAL模式）
- 数据库迁移脚本 — 版本化schema管理
- `uuid` 10.0.0 — 主键生成

**🎯 推荐Skills**: `senior-fullstack`（数据库设计）、`systematic-debugging`（SQLite性能调优）

**数据库表设计**:
```sql
-- 批改历史记录表
CREATE TABLE IF NOT EXISTS history (
  id TEXT PRIMARY KEY,
  sql_content TEXT NOT NULL,
  dialect TEXT NOT NULL,
  file_name TEXT,
  result_json TEXT NOT NULL,  -- 完整批改结果JSON
  score INTEGER,              -- 评分 0-100
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

-- 自定义规则表
CREATE TABLE IF NOT EXISTS custom_rules (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT,
  category TEXT NOT NULL CHECK(category IN ('syntax','logic','performance')),
  dialect TEXT NOT NULL,
  expression TEXT NOT NULL,
  severity TEXT NOT NULL CHECK(severity IN ('error','warning','info')),
  priority TEXT NOT NULL CHECK(priority IN ('high','medium','low')),
  enabled INTEGER NOT NULL DEFAULT 1,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

---

#### 模块 11: 自动更新服务

| 属性 | 内容 |
|------|------|
| **模块ID** | M11-AutoUpdater |
| **物理文件路径** | `src/main/services/auto-updater.service.ts` |
| **核心职责** | 检查更新、下载进度通知、安装更新 |
| **对外API** | `checkForUpdates(): Promise<UpdateInfo | null>`, `downloadUpdate(): Promise<void>`, `installUpdate(): void` |
| **内部技术** | electron-updater 6.3.0, electron-log(更新日志) |
| **交互流程** | 启动→自动检查→发现新版本→弹出通知→用户确认→下载→校验签名→安装→重启应用 |

**🔧 核心技术栈**:
- `electron-updater` 6.3.0 — 自动更新检查、下载、安装
- `electron-log` — 更新日志记录

**🎯 推荐Skills**: `senior-fullstack`（更新服务器配置）

---

#### 模块 12: 主布局组件

| 属性 | 内容 |
|------|------|
| **模块ID** | M12-MainLayout |
| **物理文件路径** | `src/renderer/components/layout/MainLayout.tsx` |
| **核心职责** | 应用主框架布局，包含菜单栏、工具栏、状态栏，管理和协调各面板的显示 |
| **对外API** | Props: `{ children: ReactNode }`; 提供PanelContext用于面板间通信 |
| **内部技术** | React 18.3, Ant Design 5.x Layout组件, CSS Modules |
| **交互流程** | 渲染Shell→加载工具栏→加载编辑区/结果区分栏面板→响应面板拖拽事件 |

**🔧 核心技术栈**:
- `react` 18.3.1 — 组件化UI
- `antd` 5.21.0 — Layout、Menu、Toolbar组件
- CSS Modules / CSS Variables — 主题切换
- 自研 `ResizablePanel` — 拖拽分隔线调整面板宽度

**🎯 推荐Skills**: `ui-ux-pro-max`（性能优化）、`react-ui-patterns`（布局模式）、`tailwind-patterns`（CSS变量主题）

---

#### 模块 13: SQL编辑器组件

| 属性 | 内容 |
|------|------|
| **模块ID** | M13-SQLEditor |
| **物理文件路径** | `src/renderer/components/editor/SQLEditor.tsx` |
| **核心职责** | Monaco Editor封装，提供SQL语法高亮、自动补全、错误标记、代码折叠 |
| **对外API** | Props: `{ value, onChange, dialect, errors, onCursorChange }`; Exposed methods: `getValue()`, `setValue()`, `revealPosition()` |
| **内部技术** | @monaco-editor/react 4.6, monaco-typescript SQL自定义Token, setModelMarkers |
| **交互流程** | 编辑器内容变更→防抖500ms→调用sql:parse IPC→接收错误→setModelMarkers标记波浪线 |

**🔧 核心技术栈**:
- `@monaco-editor/react` 4.6.0 — React Monaco封装
- `monaco-editor` 0.50.0 — VS Code内核编辑器
- `monaco.languages.registerCompletionItemProvider` — SQL自动补全
- `monaco.editor.setModelMarkers` — 错误波浪线标记

**🎯 推荐Skills**: `react-best-practices`（Monaco集成优化）、`systematic-debugging`（编辑器性能问题）

---

#### 模块 14: 批改结果展示组件

| 属性 | 内容 |
|------|------|
| **模块ID** | M14-AnalyzeResult |
| **物理文件路径** | `src/renderer/components/results/AnalyzeResultPanel.tsx` |
| **核心职责** | 分Tab展示语法错误、逻辑分析、性能建议、学习资料 |
| **对外API** | Props: `{ result: AnalyzeResult, onErrorClick: (line: number) => void }` |
| **内部技术** | Ant Design Tabs, Collapse, Tag; react-syntax-highlighter(代码块) |
| **交互流程** | 接收AnalyzeResult→分Tab渲染→用户点击错误条目→触发onErrorClick→编辑器跳转 |

**🔧 核心技术栈**:
- `antd` 5.21.0 — Tabs、Collapse、Tag、Badge组件
- `react-syntax-highlighter` 15.5.0 — 优化建议中的代码块高亮
- `react` 18.3.1 — `useTransition` 优化大结果集渲染

**🎯 推荐Skills**: `react-best-practices`（大数据渲染优化）、`react-ui-patterns`（错误/加载/空状态处理）

---

#### 模块 15: 历史记录页面

| 属性 | 内容 |
|------|------|
| **模块ID** | M15-HistoryPage |
| **物理文件路径** | `src/renderer/pages/HistoryPage.tsx` |
| **核心职责** | 展示批改历史记录列表，支持搜索、筛选、查看详情、删除 |
| **对外API** | 无外部Props，通过Zustand store获取数据 |
| **内部技术** | Ant Design Table, DatePicker; Zustand historyStore; 虚拟滚动(react-window) |
| **交互流程** | 页面加载→调用storage:history:list→渲染表格→点击行→展开详情→可选重新批改 |

**🔧 核心技术栈**:
- `antd` 5.21.0 — Table、DatePicker、Input.Search
- `react-window` 1.8.10 — 虚拟滚动（万级记录流畅渲染）
- `zustand` 5.0.0 — `useHistoryStore`

**🎯 推荐Skills**: `react-best-practices`（虚拟滚动集成）、`react-ui-patterns`（列表加载/空状态/错误状态）

---

#### 模块 16: 设置页面

| 属性 | 内容 |
|------|------|
| **模块ID** | M16-SettingsPage |
| **物理文件路径** | `src/renderer/pages/SettingsPage.tsx` |
| **核心职责** | 主题/字体/编辑器设置、数据库连接配置、自定义规则管理、AI模型配置 |
| **对外API** | 无外部Props，通过configStore + ruleStore获取状态 |
| **内部技术** | Ant Design Form, Tabs, ColorPicker; electron-store(持久化) |
| **交互流程** | 选项变更→调用config:set IPC→更新Zustand store→实时应用主题/字体变更 |

**🔧 核心技术栈**:
- `antd` 5.21.0 — Form、Tabs、ColorPicker、Select
- `electron-store` 10.0.0 — 配置持久化（JSON文件）
- `zustand` 5.0.0 — `useConfigStore`、`useRuleStore`、`useDBStore`

**🎯 推荐Skills**: `react-best-practices`（表单性能）、`react-ui-patterns`（设置页UX模式）、`api-security-best-practices`（敏感配置加密）


---

#### 模块 17: 批量批改页面

| 属性 | 内容 |
|------|------|
| **模块ID** | M17-BatchPage |
| **物理文件路径** | `src/renderer/pages/BatchPage.tsx` |
| **核心职责** | 批量文件选择、批改参数设置、进度展示、批量结果总览 |
| **对外API** | 无外部Props，通过batchStore获取状态 |
| **内部技术** | Ant Design Upload(目录选择), Progress; P-Queue进度回调 |
| **交互流程** | 选择文件→设置参数→调用batch:start→实时接收进度→展示文件级结果列表→生成汇总报告 |

**🔧 核心技术栈**:
- `antd` 5.21.0 — Upload（目录选择）、Progress、Table
- `zustand` 5.0.0 — `useBatchStore`
- `p-queue` 9.0.0 — 主进程并发控制，渲染进程通过IPC接收进度

**🎯 推荐Skills**: `react-best-practices`（进度更新优化）、`react-ui-patterns`（批量操作UX模式）、`systematic-debugging`（并发任务调试）

---

#### 模块 18: 自定义规则编辑器

| 属性 | 内容 |
|------|------|
| **模块ID** | M18-RuleEditor |
| **物理文件路径** | `src/renderer/components/settings/RuleEditor.tsx` |
| **核心职责** | 自定义规则的创建、编辑、测试、启用/禁用管理 |
| **对外API** | Props: `{ rule?: CustomRule; onSave: (rule: CustomRule) => void; onCancel: () => void }` |
| **内部技术** | Ant Design Modal, Form, Select, Switch; Monaco Editor(规则表达式编辑) |
| **交互流程** | 用户输入规则→点击测试→调用解析器验证规则有效性→保存到SQLite→刷新规则列表 |

**🔧 核心技术栈**:
- `antd` 5.21.0 — Modal、Form、Select、Switch、Input
- `@monaco-editor/react` 4.6.0 — 规则表达式编辑（内嵌Monaco Editor）
- `zustand` 5.0.0 — `useRuleStore`

**🎯 推荐Skills**: `react-best-practices`（表单验证）、`react-ui-patterns`（Modal/表单UX模式）

---

#### 模块 19: 规则命中来源标记与可解释性增强

| 属性 | 内容 |
|------|------|
| **模块ID** | M19-RuleHitTrace |
| **物理文件路径** | `src/renderer/App.tsx`、`src/renderer/components/results/*.tsx` |
| **核心职责** | 在结果面板中标识命中来源（内置规则/自定义规则），提升可解释性与排查效率 |
| **对外API** | 扩展结果项字段：`source: 'builtin' \| 'custom'`（建议） |
| **内部技术** | RuleMatch 映射扩展、Antd Tag 标记来源、按来源筛选展示 |
| **交互流程** | 规则命中→映射结果项→显示来源Tag→用户可快速定位规则来源 |

**🔧 核心技术栈**:
- `TypeScript` 判别联合类型
- `antd` Tag、Tooltip
- `React` 条件渲染

**🎯 推荐Skills**: `react-ui-patterns`（结果可解释性）、`systematic-debugging`（命中误报排查）

---

#### 模块 20: 规则测试沙箱（真实命中测试）

| 属性 | 内容 |
|------|------|
| **模块ID** | M20-RuleSandbox |
| **物理文件路径** | `src/renderer/components/settings/RuleEditor.tsx`、`src/main/ipc/storage-handlers.ts` |
| **核心职责** | 对“规则 + 测试SQL”执行真实命中评估，不止做语法解析校验 |
| **对外API** | 新增IPC建议：`rule:test` 入参 `{ rule, sql, dialect }` 出参 `RuleMatch[]` |
| **内部技术** | 复用 `rule-engine.service.ts` + `sql-parser.service.ts` |
| **交互流程** | 用户输入规则→输入测试SQL→点击测试→返回命中条目与位置 |

**🔧 核心技术栈**:
- `rule-engine.service` 命中计算
- `node-sql-parser` AST生成
- `antd` Alert/Table

**🎯 推荐Skills**: `systematic-debugging`（链路验证）、`react-best-practices`（测试面板交互）

---

#### 模块 21: 批改中断控制（停止批改）

| 属性 | 内容 |
|------|------|
| **模块ID** | M21-AnalyzeCancel |
| **物理文件路径** | `src/renderer/App.tsx`、`src/main/services/ai-analyze.service.ts`、`src/main/ipc/sql-handlers.ts` |
| **核心职责** | 实现分析过程可取消，满足说明书“停止批改”要求 |
| **对外API** | 新增IPC建议：`sql:analyze:cancel` 或 `sql:ai-analyze` 支持 `requestId` |
| **内部技术** | AbortController、请求令牌、任务状态机 |
| **交互流程** | 开始批改→生成请求ID→点击停止→主进程取消→前端恢复可操作状态 |

**🔧 核心技术栈**:
- `AbortController`
- `LangChain/OpenAI` 请求中断策略
- `React` loading状态管理

**🎯 推荐Skills**: `systematic-debugging`（竞态问题排查）、`performance-engineer`（取消后的资源回收）

---

#### 模块 22: 菜单/入口行为一致性校验

| 属性 | 内容 |
|------|------|
| **模块ID** | M22-EntryConsistency |
| **物理文件路径** | `src/renderer/App.tsx`、`src/renderer/components/layout/Toolbar.tsx`、`src/main/menu-builder.ts` |
| **核心职责** | 确保顶部按钮、菜单项、页面入口行为完全一致 |
| **对外API** | 无 |
| **内部技术** | 统一导航函数、菜单动作分发、入口回归测试 |
| **交互流程** | 任一入口触发设置/历史/批量→调用同一处理函数→行为一致 |

**🔧 核心技术栈**:
- `React` 单一状态源（view）
- `Electron Menu` action dispatch
- `Playwright` 入口一致性E2E

**🎯 推荐Skills**: `karpathy-guidelines`（外科手术式修改）、`systematic-debugging`（入口失效排查）

---

#### 模块 23: 自动更新闭环验收

| 属性 | 内容 |
|------|------|
| **模块ID** | M23-UpdaterAcceptance |
| **物理文件路径** | `src/main/services/auto-updater.service.ts`、`electron-builder.yml` |
| **核心职责** | 在打包环境验证检查更新、下载、安装、重启全流程 |
| **对外API** | `onUpdateEvent` 事件流（检查中/可更新/进度/安装完成） |
| **内部技术** | electron-updater 事件监听与渠道配置 |
| **交互流程** | 启动→检查更新→提示下载→展示进度→安装重启 |

**🔧 核心技术栈**:
- `electron-updater`
- `electron-builder` publish配置
- 事件驱动通知

**🎯 推荐Skills**: `deployment-procedures`（发布与回滚）、`systematic-debugging`（更新故障排查）

---

#### 模块 24: 说明书验收矩阵与整改计划

| 属性 | 内容 |
|------|------|
| **模块ID** | M24-DocAcceptanceMatrix |
| **物理文件路径** | `技术文档.md`（本节） + `tests/e2e/*.spec.ts` |
| **核心职责** | 将说明书条款映射成可执行验收项，形成P0/P1/P2整改路线 |
| **对外API** | 无 |
| **内部技术** | 验收矩阵、E2E自动化、缺陷优先级管理 |
| **交互流程** | 读取说明书→映射功能点→执行测试→输出差距报告→迭代收口 |

**🔧 核心技术栈**:
- `Playwright` E2E
- `Vitest` 集成测试
- Markdown验收清单

**🎯 推荐Skills**: `concise-planning`（任务拆解）、`systematic-debugging`（失败项复盘）

---

#### 模块 25: 参考答案对比

| 属性 | 内容 |
|------|------|
| **模块ID** | M25-ReferenceDiff |
| **物理文件路径** | `src/renderer/components/results/ReferenceDiff.tsx`、`src/renderer/App.tsx` |
| **核心职责** | 教师提供标准SQL，将学生SQL与标准答案做结构化和语义级对比 |
| **对外API** | Props: `{ studentSql: string; referenceSql: string; dialect: Dialect }`; Exposed: `getDiffResult(): DiffResult` |
| **内部技术** | Monaco Editor diff mode（side-by-side对比）、AST结构对比（node-sql-parser解析后按表/字段/条件逐项比对）、差异高亮与分类（语法差异/逻辑差异/性能差异） |
| **交互流程** | 教师粘贴标准SQL→点击"对比"→并行解析两份AST→按层级（表引用→字段→JOIN→WHERE→GROUP BY）逐项diff→输出差异报告含相似度评分 |

**🔧 核心技术栈**:
- `@monaco-editor/react` 4.6.0 — `monaco.editor.createDiffEditor` 双栏对比
- `node-sql-parser` 5.3.0 — AST结构提取与节点对比
- `antd` Collapse / Tree — 差异树形展示
- TypeScript 判别联合类型 — 差异类型分类

**🎯 推荐Skills**: `react-best-practices`（Monaco diff集成）、`systematic-debugging`（AST diff逻辑排查）、`typescript-expert`（差异类型系统设计）

---

#### 模块 26: PDF 报告导出

| 属性 | 内容 |
|------|------|
| **模块ID** | M26-PDFExport |
| **物理文件路径** | `src/main/services/report-generator.service.ts`、`src/main/ipc/report-handlers.ts`、`templates/report-single.hbs` |
| **核心职责** | 将单次批改结果渲染为HTML报告并导出PDF（含语法/逻辑/性能/学习四维结果、评分、代码片段） |
| **对外API** | `generateReport(result: AnalyzeResult, sql: string, options: ReportOptions): Promise<string>`（返回PDF文件路径） |
| **内部技术** | Handlebars 4.7模板引擎渲染HTML、Puppeteer 23.x HTML→PDF转换（无头Chromium）、报告CSS与前端主题色联动、页眉页脚（文件名/日期/页码） |
| **交互流程** | 批改完成→用户点击"导出报告"→渲染Handlebars模板（注入批改结果+SQL代码+评分+主题色）→Puppeteer转PDF→保存到用户指定路径→可选自动打开 |

**🔧 核心技术栈**:
- `handlebars` 4.7.8 — HTML模板渲染
- `puppeteer-core` 23.5.0 — 无头Chromium HTML→PDF
- `antd` 导出按钮 + `message.success`
- 报告CSS — 与前端主题变量联动

**🎯 推荐Skills**: `senior-fullstack`（PDF生成管道）、`tailwind-patterns`（报告样式设计）、`systematic-debugging`（Puppeteer渲染异常排查）

---

#### 模块 27: 批量汇总报告

| 属性 | 内容 |
|------|------|
| **模块ID** | M27-BatchSummaryReport |
| **物理文件路径** | `src/main/services/report-generator.service.ts`、`src/renderer/pages/BatchPage.tsx`、`templates/report-batch.hbs` |
| **核心职责** | 批量批改完成后，将全部文件结果汇总为一份概览报告（含总体统计、各文件评分排行、共性问题聚合、知识点薄弱项分析） |
| **对外API** | `generateBatchReport(batchId: string, format: 'pdf'|'html'): Promise<string>` |
| **内部技术** | Handlebars汇总模板、各文件评分分布图表（Chart.js生成PNG嵌入报告）、共性问题聚合算法（按错误类型聚类）、排名表渲染 |
| **交互流程** | 批量批改完成→点击"汇总报告"→聚合所有BatchTaskResult→统计平均分/及格率/高频错误→渲染汇总模板→Puppeteer导出PDF/HTML |

**🔧 核心技术栈**:
- `handlebars` 4.7.8 — 汇总模板
- `chart.js` + `chartjs-node-canvas` — 评分分布/错误类型统计图
- `puppeteer-core` 23.5.0 — PDF导出
- `antd` Table — 排名表预览

**🎯 推荐Skills**: `senior-fullstack`（汇总管道设计）、`react-best-practices`（图表组件）、`performance-engineer`（大结果集聚合性能）

---

#### 模块 28: 数据库元数据注入AI

| 属性 | 内容 |
|------|------|
| **模块ID** | M28-DBMetadataAI |
| **物理文件路径** | `src/main/services/db-connection.service.ts`、`src/main/services/ai-analyze.service.ts`、`src/main/ipc/sql-handlers.ts`、`src/renderer/components/settings/DBConfigForm.tsx` |
| **核心职责** | 连接真实数据库→采集表结构/字段类型/索引/约束信息→注入AI Prompt→让AI基于真实Schema做逻辑和性能分析 |
| **对外API** | `fetchMetadata(connId: string): Promise<DBMetadata>`; `analyze(context: AnalyzeContext): Promise<AnalyzeResult>`（context.metadata 为新增入参） |
| **内部技术** | mysql2/pg/mssql驱动查询INFORMATION_SCHEMA/pg_catalog、元数据缓存（5分钟TTL）、Prompt注入（将表结构→DDL文本→嵌入AI system prompt）、字段类型校验增强 |
| **交互流程** | 用户配置数据库连接→点击"连接"→采集元数据（表名/字段/类型/索引/外键）→缓存→批改时自动将元数据注入AnalyzeContext→AI基于真实Schema分析（检查字段类型不匹配、缺失索引、外键约束违反） |

**🔧 核心技术栈**:
- `mysql2` 3.11.0 / `pg` 8.13.0 / `mssql` 11.0.0 — INFORMATION_SCHEMA查询
- `@langchain/core` 0.3.0 — PromptTemplate动态注入DDL文本
- `zustand` 5.0.0 — `useDBStore` 连接状态管理
- `encryption.service.ts` — OS原生密钥链加密存储密码

**🎯 推荐Skills**: `api-security-best-practices`（连接信息加密）、`python-pro`（Oracle驱动编译）、`systematic-debugging`（元数据采集异常排查）、`performance-engineer`（元数据缓存策略）

---

### 模块 24 附：说明书功能对照（当前版本）

| 项目 | 结论 | 说明 |
|------|------|------|
| SQL编辑/打开/保存/格式化 | ✅ 符合 | 主流程稳定 |
| 语法/逻辑/性能/学习四类结果 | ✅ 符合 | 已联动展示 |
| 历史记录保存/查看/回填 | ✅ 符合 | 已修复保存与加载 |
| 自定义规则增删改启用 | ✅ 符合 | 设置页可用 |
| 自定义规则影响结果与计分 | ✅ 符合 | 已接入 `App.tsx` |
| 规则测试（真实命中） | ⚠️ 部分符合 | 现为解析校验，建议补M20 |
| 停止批改 | ❌ 待补齐 | 建议实现M21 |
| 批量批改闭环验收 | ⚠️ 部分符合 | 建议补E2E验收脚本 |
| 自动更新闭环 | ⚠️ 部分符合 | 需打包环境验证 |
| 多入口行为一致性 | ⚠️ 部分符合 | 顶部设置已修，建议补回归测试 |

---

## 四、项目工程化终极指南

### 4.1 完整项目目录树

```
sql-ai-grader/
├── .env.example                          # 环境变量示例
├── .eslintrc.cjs                         # ESLint配置
├── .prettierrc                           # Prettier配置
├── electron-builder.yml                  # 打包配置
├── package.json                          # 依赖与脚本
├── tsconfig.json                         # 主TypeScript配置
├── tsconfig.node.json                    # 主进程TypeScript配置
├── tsconfig.web.json                     # 渲染进程TypeScript配置
├── vite.config.ts                        # Vite构建配置
├── vitest.config.ts                      # Vitest测试配置
├── playwright.config.ts                  # Playwright E2E配置
│
├── resources/                            # 静态资源
│   ├── icon.ico                          # Windows图标
│   ├── icon.icns                         # macOS图标
│   └── icon.png                          # Linux图标
│
├── src/
│   ├── shared/                           # 主进程与渲染进程共享类型
│   │   ├── types/
│   │   │   ├── index.ts                  # 统一导出
│   │   │   ├── ipc-channels.ts           # IPC通道类型定义
│   │   │   ├── sql-types.ts              # SQL相关类型(方言/解析结果/AST)
│   │   │   ├── ai-types.ts               # AI分析结果类型
│   │   │   ├── db-types.ts               # 数据库连接/元数据类型
│   │   │   ├── rule-types.ts             # 规则类型
│   │   │   ├── batch-types.ts            # 批量任务类型
│   │   │   └── storage-types.ts          # 存储记录类型
│   │   └── constants/
│   │       ├── dialects.ts               # 方言列表与描述
│   │       └── defaults.ts               # 默认配置常量
│   │
│   ├── main/                             # 主进程代码
│   │   ├── index.ts                      # M01: 主进程入口
│   │   ├── window-manager.ts             # 窗口管理器
│   │   ├── menu-builder.ts               # 原生菜单构建
│   │   ├── ipc/
│   │   │   ├── index.ts                  # M02: IPC统一注册
│   │   │   ├── sql-handlers.ts           # SQL相关IPC处理器
│   │   │   ├── db-handlers.ts            # 数据库相关IPC处理器
│   │   │   ├── batch-handlers.ts         # 批量任务IPC处理器
│   │   │   ├── report-handlers.ts        # 报告生成IPC处理器
│   │   │   ├── rule-handlers.ts          # 规则管理IPC处理器
│   │   │   ├── storage-handlers.ts       # 存储IPC处理器
│   │   │   ├── config-handlers.ts        # 配置IPC处理器
│   │   │   └── file-handlers.ts          # 文件操作IPC处理器
│   │   ├── services/
│   │   │   ├── sql-parser.service.ts     # M03: SQL解析服务
│   │   │   ├── ai-analyze.service.ts     # M04: AI批改服务
│   │   │   ├── db-connection.service.ts  # M05: 数据库连接服务
│   │   │   ├── encryption.service.ts     # M06: 加密服务
│   │   │   ├── rule-engine.service.ts    # M07: 规则引擎
│   │   │   ├── batch-queue.service.ts    # M08: 批量任务队列
│   │   │   ├── report-generator.service.ts # M09: 报告生成
│   │   │   ├── storage.service.ts        # M10: 本地存储服务
│   │   │   └── auto-updater.service.ts   # M11: 自动更新
│   │   └── database/
│   │       └── migrations/
│   │           ├── 001-create-history.ts  # 历史记录表迁移
│   │           └── 002-create-rules.ts    # 自定义规则表迁移
│   │
│   ├── preload/
│   │   └── index.ts                      # M02: contextBridge安全API暴露
│   │
│   ├── renderer/                         # 渲染进程代码
│   │   ├── index.html                    # HTML入口
│   │   ├── main.tsx                      # React入口
│   │   ├── App.tsx                       # 根组件(路由+布局)
│   │   ├── styles/
│   │   │   ├── global.css                # 全局样式
│   │   │   ├── themes/
│   │   │   │   ├── light.css             # 浅色主题CSS变量
│   │   │   │   └── dark.css              # 深色主题CSS变量
│   │   │   └── editor.css                # 编辑器覆盖样式
│   │   ├── components/
│   │   │   ├── layout/
│   │   │   │   ├── MainLayout.tsx        # M12: 主布局
│   │   │   │   ├── Toolbar.tsx           # 工具栏组件
│   │   │   │   ├── StatusBar.tsx         # 状态栏组件
│   │   │   │   └── ResizablePanel.tsx    # 可拖拽面板
│   │   │   ├── editor/
│   │   │   │   ├── SQLEditor.tsx         # M13: SQL编辑器
│   │   │   │   └── EditorToolbar.tsx     # 编辑器内工具栏
│   │   │   ├── results/
│   │   │   │   ├── AnalyzeResultPanel.tsx # M14: 批改结果面板
│   │   │   │   ├── SyntaxErrorsTab.tsx   # 语法错误Tab
│   │   │   │   ├── LogicAnalysisTab.tsx  # 逻辑分析Tab
│   │   │   │   ├── PerformanceTab.tsx    # 性能建议Tab
│   │   │   │   └── LearningTab.tsx       # 学习资料Tab
│   │   │   └── settings/
│   │   │       ├── RuleEditor.tsx        # M18: 规则编辑器
│   │   │       └── DBConfigForm.tsx      # 数据库配置表单
│   │   ├── pages/
│   │   │   ├── EditorPage.tsx            # 主编辑页(提交作业)
│   │   │   ├── HistoryPage.tsx           # M15: 历史记录页
│   │   │   ├── SettingsPage.tsx          # M16: 设置页
│   │   │   └── BatchPage.tsx             # M17: 批量批改页
│   │   ├── stores/                       # Zustand状态管理
│   │   │   ├── useEditorStore.ts         # 编辑器状态
│   │   │   ├── useResultStore.ts         # 批改结果状态
│   │   │   ├── useHistoryStore.ts        # 历史记录状态
│   │   │   ├── useConfigStore.ts         # 配置状态(主题/字体/方言)
│   │   │   ├── useRuleStore.ts           # 规则状态
│   │   │   ├── useDBStore.ts             # 数据库连接状态
│   │   │   └── useBatchStore.ts          # 批量任务状态
│   │   └── hooks/
│   │       ├── useIPC.ts                 # IPC调用Hook封装
│   │       ├── useTheme.ts               # 主题Hook
│   │       └── useDebounce.ts            # 防抖Hook
│   │
│   └── workers/                          # Web Workers
│       └── ai-worker.ts                  # AI推理Worker(可选)
│
├── tests/
│   ├── unit/
│   │   ├── sql-parser.test.ts            # SQL解析器单元测试
│   │   ├── rule-engine.test.ts           # 规则引擎单元测试
│   │   ├── encryption.test.ts            # 加密服务单元测试
│   │   └── batch-queue.test.ts           # 队列服务单元测试
│   ├── integration/
│   │   ├── ipc-handlers.test.ts          # IPC处理器集成测试
│   │   ├── storage-service.test.ts       # 存储服务集成测试
│   │   └── db-connection.test.ts         # 数据库连接集成测试
│   └── e2e/
│       ├── editor.spec.ts                # 编辑器交互E2E
│       ├── analyze.spec.ts               # 批改流程E2E
│       ├── batch.spec.ts                 # 批量批改E2E
│       └── settings.spec.ts              # 设置页E2E
│
├── templates/                            # Handlebars模板
│   ├── report-single.hbs                 # 单文件报告模板
│   ├── report-batch.hbs                  # 批量汇总报告模板
│   └── report.css                        # 报告样式
│
└── scripts/                              # 构建辅助脚本
    ├── init-db.ts                        # 数据库初始化脚本
    └── download-antlr-grammars.ts        # ANTLR语法文件下载
```

### 4.2 `package.json` 完整内容

```json
{
  "name": "sql-ai-grader",
  "version": "1.0.0",
  "description": "SQL AI智批改软件 - 基于AI的SQL语句智能批改工具",
  "main": "dist/main/index.js",
  "author": "School",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "lint": "eslint src/ --ext .ts,.tsx --fix",
    "lint:check": "eslint src/ --ext .ts,.tsx",
    "format": "prettier --write \"src/**/*.{ts,tsx,css,json}\"",
    "format:check": "prettier --check \"src/**/*.{ts,tsx,css,json}\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "package": "electron-vite build && electron-builder",
    "package:win": "electron-vite build && electron-builder --win",
    "package:mac": "electron-vite build && electron-builder --mac",
    "package:linux": "electron-vite build && electron-builder --linux",
    "db:init": "tsx scripts/init-db.ts",
    "postinstall": "electron-builder install-app-deps"
  },
  "dependencies": {
    "electron-updater": "6.3.0",
    "electron-store": "10.0.0",
    "better-sqlite3": "11.3.0",
    "mysql2": "3.11.0",
    "pg": "8.13.0",
    "mssql": "11.0.0",
    "node-sql-parser": "5.3.0",
    "sql-formatter": "15.4.0",
    "@langchain/core": "0.3.0",
    "@langchain/openai": "0.3.0",
    "openai": "4.68.0",
    "langchain": "0.3.0",
    "p-queue": "9.0.0",
    "handlebars": "4.7.8",
    "puppeteer-core": "23.5.0",
    "crypto-js": "4.2.0",
    "uuid": "10.0.0",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "electron": "31.5.0",
    "electron-vite": "2.3.0",
    "electron-builder": "25.1.0",
    "@electron-toolkit/tsconfig": "1.0.1",
    "@electron-toolkit/eslint-config-ts": "2.0.0",
    "@electron-toolkit/preload": "3.0.1",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "@types/react": "18.3.10",
    "@types/react-dom": "18.3.0",
    "@monaco-editor/react": "4.6.0",
    "monaco-editor": "0.50.0",
    "antd": "5.21.0",
    "@ant-design/icons": "5.5.0",
    "zustand": "5.0.0",
    "react-router-dom": "6.27.0",
    "@tanstack/react-query": "5.59.0",
    "react-window": "1.8.10",
    "react-syntax-highlighter": "15.5.0",
    "clsx": "2.1.1",
    "dayjs": "1.11.13",
    "typescript": "5.5.4",
    "@types/node": "22.5.0",
    "@types/better-sqlite3": "7.6.11",
    "@types/pg": "8.11.10",
    "@types/uuid": "10.0.0",
    "@types/crypto-js": "4.2.2",
    "vitest": "2.1.0",
    "@vitest/coverage-v8": "2.1.0",
    "playwright": "1.47.0",
    "@playwright/test": "1.47.0",
    "eslint": "8.57.0",
    "prettier": "3.3.3",
    "tsx": "4.19.0",
    "electron-devtools-installer": "3.2.0"
  }
}
```

### 4.3 构建配置 (`electron-builder.yml`)

```yaml
appId: com.school.sql-ai-grader
productName: SQL AI智批改
copyright: Copyright © 2024 School

directories:
  output: dist-electron
  buildResources: resources

files:
  - dist/**/*
  - node_modules/**/*
  - "!node_modules/**/docs/**"
  - "!node_modules/**/test/**"
  - "!node_modules/**/tests/**"
  - "!node_modules/**/*.md"
  - "!node_modules/**/*.map"

asar: true
asarUnpack:
  - node_modules/node-sql-parser/**
  - node_modules/better-sqlite3/**
  - node_modules/puppeteer-core/**
  - node_modules/oracledb/**  # Oracle驱动需解压
  - resources/**

extraMetadata:
  main: dist/main/index.js

win:
  target:
    - target: nsis
      arch:
        - x64
        - ia32
  icon: resources/icon.ico
  artifactName: "${productName}-Setup-${version}-${arch}.${ext}"

nsis:
  oneClick: false
  perMachine: true
  allowToChangeInstallationDirectory: true
  shortcutName: "SQL AI智批改"
  deleteAppDataOnUninstall: false
  installerIcon: resources/icon.ico

mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
  icon: resources/icon.icns
  category: public.app-category.developer-tools
  hardenedRuntime: true
  entitlements: build/entitlements.mac.plist
  artifactName: "${productName}-${version}-${arch}.${ext}"

dmg:
  title: "${productName} ${version}"
  icon: resources/icon.icns

linux:
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
  icon: resources/icon.png
  category: Development
  artifactName: "${productName}-${version}-${arch}.${ext}"

publish:
  provider: generic
  url: https://updates.your-school.edu.cn/releases/
  channel: latest
```

### 4.4 环境变量与配置

创建 `.env.example` 文件：

```env
# ==============================
# SQL AI智批改 - 环境变量配置
# ==============================

# ---- AI 模型配置 ----
# DeepSeek API Key（必填，用于AI批改功能）
# 获取地址: https://platform.deepseek.com/api_keys
AI_API_KEY=sk-your-deepseek-api-key-here

# AI模型选择（默认使用deepseek-chat，能力等同GPT-4o，中文更强）
AI_MODEL=deepseek-chat

# API Base URL（DeepSeek完全兼容OpenAI SDK协议）
AI_API_BASE_URL=https://api.deepseek.com/v1

# 最大Token数（控制AI回复长度）
AI_MAX_TOKENS=4096

# 请求超时时间(ms)
AI_TIMEOUT=60000

# 可选：本地模型配置（Ollama等，作为DeepSeek的离线备选）
# LOCAL_MODEL_ENABLED=true
# LOCAL_MODEL_URL=http://localhost:11434
# LOCAL_MODEL_NAME=qwen2.5:7b

# ---- 数据库加密密钥 ----
# 用于加密存储的数据库连接密码（32字符十六进制）
DB_ENCRYPTION_KEY=your-32-char-hex-encryption-key

# ---- 更新服务器配置 ----
# 自动更新检查的发布服务器URL
UPDATE_SERVER_URL=https://updates.your-school.edu.cn/releases/

# ---- 日志配置 ----
# 日志级别: debug | info | warn | error
LOG_LEVEL=info

# 日志文件最大大小(MB)
LOG_MAX_SIZE=10

# ---- 批改配置 ----
# 批量批改最大并发数
BATCH_CONCURRENCY=3

# 编辑器实时检查防抖时间(ms)
EDITOR_DEBOUNCE_MS=500

# ---- 数据目录（可选，默认使用app.getPath('userData')） ----
# DATA_DIR=/custom/path/to/data
```

### 4.5 从零启动的"傻瓜式"指南

```markdown
# SQL AI智批改 — 从零启动指南

## 前置条件
- Node.js 20.x LTS（推荐 20.17.0 及以上）
- npm 10.x
- Git
- （可选）Python 3.10+ （仅供Oracle驱动编译使用）

## 步骤1：克隆项目并安装依赖
```bash
git clone <your-repo-url> sql-ai-grader
cd sql-ai-grader
npm install
```

## 步骤2：配置环境变量
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件，填入你的 DeepSeek API Key（从 https://platform.deepseek.com/api_keys 获取）
# AI_API_KEY=sk-your-actual-key
```

## 步骤3：初始化本地数据库
```bash
npm run db:init
# 这将在用户数据目录创建SQLite数据库，包含history和custom_rules表
```

## 步骤4：启动开发模式
```bash
npm run dev
# 等待编译完成后，Electron窗口将自动打开
# 开发模式下支持热重载(HMR)
```

## 步骤5：构建可分发的安装包
```bash
# Windows (.exe)
npm run package:win

# macOS (.dmg)
npm run package:mac

# Linux (.AppImage / .deb)
npm run package:linux

# 产物位于 dist-electron/ 目录
```

## 常用命令速查
| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发模式(HMR) |
| `npm run build` | 仅构建，不打包 |
| `npm run test` | 运行单元测试 |
| `npm run test:e2e` | 运行E2E测试 |
| `npm run lint` | ESLint自动修复 |
| `npm run format` | Prettier格式化 |
| `npm run typecheck` | TypeScript类型检查 |
```

---

## 五、关键设计决策补充

### 5.1 数据流向图

```
用户输入SQL → Monaco Editor
                  ↓ onChange(防抖500ms)
            IPC: sql:parse → SQL Parser Service
                  ↓ 生成AST
            规则引擎评估(本地静态规则)
                  ↓ 错误/警告列表
            setModelMarkers(编辑器波浪线标记)
                  ↓ 用户点击"开始批改"
            IPC: sql:ai-analyze → AI Service
                  ↓ 构建Prompt(含AST+元数据+方言)
            调用DeepSeek/LangChain
                  ↓ 结构化JSON结果
            解析 → AnalyzeResult
                  ↓
            Zustand store → AnalyzeResultPanel渲染
                  ↓ 用户选择保存
            IPC: storage:history:save → SQLite持久化
```

### 5.2 安全模型

```
┌────────────────────────────────────┐
│          RENDERER PROCESS          │
│  只能通过 preload 暴露的 API 通信   │
│  无法直接访问 Node.js API          │
└────────────────┬───────────────────┘
                 │ contextBridge 白名单
┌────────────────▼───────────────────┐
│           MAIN PROCESS             │
│  - 敏感数据(密码/API Key)加密存储   │
│  - 数据库连接在隔离的作用域管理     │
│  - 文件访问经由对话框，不可直接路径  │
└────────────────────────────────────┘
```

### 5.3 性能优化策略

1. **编辑器防抖**: SQL解析请求防抖500ms，避免频繁AST生成
2. **虚拟滚动**: 历史记录列表使用react-window，支持万级记录
3. **结果缓存**: 相同SQL+方言+元数据的批改结果缓存5分钟
4. **懒加载**: Oracle/mssql驱动按需加载，减小启动内存
5. **Worker线程**: 批量批改的AI调用在独立Worker中执行
6. **增量打包**: electron-updater支持块级增量更新，减少下载量
```

当前生成的是完整的技术文档，涵盖技术栈选型、完整目录结构、全部18个模块的设计以及工程化配置。是否需要我继续为这些模块逐个生成完整的TypeScript实现代码？
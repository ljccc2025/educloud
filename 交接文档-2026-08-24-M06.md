# 交接文档-2026-08-24-M06（educloud-content 内容模块与学习中心）

> 面向：下一位接手 EduCloud 微服务开发的工程师。  
> 模块：M06 内容与学习服务（educloud-content）及前端全流程学习中心——**已完成并验收**，本文是交接与继承指引。  
> 生成时间：2026-08-24  

---

## 0. 一句话状态

M06 内容模块**全部功能完成、通用门禁全部达标、三端联调打通、Playwright / Chrome DevTools MCP 端到端自动化验收通过**；  
真实章节大纲（Chapter）与多形态课件（Courseware：视频/文档/外链）已就绪，学生端完成**经典居中双栏卡片学习页**重构，管理端修复表格状态徽标折行 BUG；  
所有代码已全量提交并推送到 GitHub `main` 分支（Commit `cb6e43f`，已完成 Review 清单 10 项缺陷修复），VM 虚拟机（192.168.100.136）各微服务均处于 **`UP`** 状态；下一步为 **M07（educloud-order 订单中心）**。

---

## 1. 代码与文档位置

| 项目 | 位置 |
|---|---|
| 唯一权威工作区 | `D:\microservice\.worktrees\educloud-backend-foundation`（**main 分支**，git 仓库） |
| 远端仓库 | github `https://github.com/ljccc2025/educloud.git`（0 ahead / 0 behind） |
| 旧远端 | `origin` = education-system（**不要推**，仅保留镜像参考） |
| M06 设计规格 | `docs/superpowers/specs/2026-08-24-educloud-content-design.md` |
| 学习页重构规格 | `docs/superpowers/specs/2026-08-24-learning-page-layout-design.md` |
| 历史交接 | `交接文档-2026-08-22.md`（M01-M03）、`交接文档-2026-08-23-M04.md`（文件）、`交接文档-2026-08-24-M05.md`（课程） |
| 本交接文档 | `交接文档-2026-08-24-M06.md` |

**工作区纪律**：所有开发只在 worktree 进行；改完提交并推送至 github main；  
VM 上对应目录 `/root/educloud/.worktrees/educloud-backend-foundation` 是**文件副本（非 git 仓库）**，本地改动通过 `sync_to_vm.py` 脚本同步（见 §4）。

---

## 2. 模块范围与交付物

M06 = 内容服务全链路：**章节树 CRUD / 课件多形态管理 / 免费试看控制 / Presigned 临时安全播放与下载 / 防作弊学习进度心跳上报 / 课程完成度聚合 / 学生端居中双栏重构 / 管理端表格样式修复**。

### 2.1 核心业务流

- **章节与课件层级模型**：`Course` 1:N `Chapter` 1:N `Courseware`。章节与课件均支持 `sortOrder` 排序。
- **课件多形态支持**：
  - `VIDEO`（视频）：关联 `fileId` 或 `externalUrl`，记录 `durationSeconds`（秒）与视频体积；
  - `DOCUMENT`（PDF / 讲义）：关联 `fileId` 或文档外链，记录 `sizeBytes`；
  - `EXTERNAL`（外部链接）：嵌入第三方扩展教学资源。
- **免费试看权限校验**：课件 `freePreview = true` 时，匿名或未选课学员可直接获取播放地址；`freePreview = false` 时校验学员是否已选课（通过 `educloud-course` 选课记录），未选课拦截 `403 NOT_ENROLLED`。
- **Presigned 安全下载/播放**：内部 Feign 调用 `educloud-file`（端口 8087）获取安全临时签名 URL（1 小时有效期），禁止向前端暴露持久存储桶地址。
- **防作弊学习进度追踪**：
  - 学习心跳上报端点：`POST /api/v1/learning/progress`；
  - 校验学习进度不得超出课件总时长（`positionSeconds <= durationSeconds`）；
  - 记录各课件的 `completed` 状态与断点位置；
  - 聚合计算当前课程总完成度百分比（`completedCoursewareCount / totalCoursewareCount * 100`）。

### 2.2 关键端点（经网关 /api/v1）

| 端 | 端点 | 请求方式 | 说明 |
|---|---|---|---|
| 公开/学生 | `/courses/{courseId}/chapters` | GET | 获取课程章节大纲与课件树（公开放行） |
| 学生/公开 | `/coursewares/{coursewareId}/download-url` | GET | 获取课件临时播放/下载 Presigned URL（试看放行/鉴权） |
| 学生 | `/learning/progress` | POST | 上报当前课件学习进度与完成状态（带防作弊校验） |
| 学生 | `/learning/progress/{courseId}` | GET | 查询当前用户在指定课程的学习进度概览 |
| 教师 | `/teacher/courses/{courseId}/chapters` | GET/POST | 查询/新增课程章节（归属校验） |
| 教师 | `/teacher/chapters/{chapterId}` | PUT/DELETE | 修改/删除章节 |
| 教师 | `/teacher/chapters/{chapterId}/coursewares` | POST | 挂载新课件（支持视频/文档/外链） |
| 教师 | `/teacher/coursewares/{coursewareId}` | PUT/DELETE | 更新课件信息与排序 / 删除课件 |
| 管理 | `/admin/coursewares/audit` | GET/PUT | 审核教师上传的音视频与课件内容 |

### 2.3 前端三端交互升级与 Bug 修复

1. **学生端（`student-portal` : 5173）**：
   - **经典居中双栏卡片模式**：解决大屏下贴左拉满（1600px+）与巨型黑底问题，版心约束在 `max-w-7xl`（1280px）黄金居中范围，播放器严格锁定 16:9 标准比例；
   - **全链路真实数据对接**：对接真实大纲接口，支持课件多形态展示（HTML5 视频原生控件、PDF 讲义在线预览卡片、模拟播放器平滑降级）；
   - **上下节无缝切换与进度联动**：支持“上一节 / 下一节”快捷切换、点击“标记学完”实时刷新右侧进度环与完成度百分比；
   - **并发防抖锁**：为 `handleMarkComplete` 进度上报添加 `isReporting` 互斥防抖锁，防止用户快速连击造成重复请求。
2. **管理端（`admin-portal` : 5175）**：
   - 修复表格「状态」列（`已支付`、`待支付`、`已退款`）徽标被强制拆行成两行的 BUG；
   - 全局 `.badge` 样式与 `DataTable` 单元格注入 `whitespace-nowrap shrink-0` 与显式宽度保护。
3. **教师端（`teacher-portal` : 5174）**：
   - 联动课件内容管理面板，支持章节与多课件管理。

### 2.4 权限与数据迁移

- **数据库迁移**：
  - `content` 库：`deploy/sql/content/V000__technical_tables.sql`（`chapter`, `courseware`, `user_courseware_progress`, `user_course_progress`）；`V001__content_schema.sql`；`V002__content_seed.sql`（预置章节课件种子数据）。
  - `user` 库：`deploy/sql/user/V006__content_permissions.sql`（新增 `content:create/update/delete/audit` 权限码 **111-114**）。
- **网关放行契约**：
  - `educloud-gateway/AccessPolicy.java`：已将 `/api/v1/courses/{courseId}/chapters` 与 `/api/v1/coursewares/{coursewareId}/download-url` 加入 `PUBLIC_READ` 白名单。

---

## 3. 技术栈与架构契约

- **技术栈**：Java 17 / Spring Boot 3.2.5 / MyBatis-Plus 3.5.12 / MySQL 8.0.36 / Redis / RabbitMQ / Nacos / MinIO / React 18 / Tailwind CSS。
- **微服务端口规划（双端口模式）**：
  - `educloud-gateway`：`8080`（业务） / `8081`（监控）
  - `educloud-user`：`8082`（业务） / `8083`（监控）
  - `educloud-content` (M06)：`8085`（业务） / `8086`（监控）
  - `educloud-file`：`8087`（业务） / `8088`（监控）
  - `educloud-course`：`8089`（业务） / `8090`（监控）
  - 前端三端：`5173`（学生） / `5174`（教师） / `5175`（管理）
- **核心组件配置**：
  - `IdentifierConfig.java`：基于 `IdWorker::getId` 注入 MyBatis-Plus Snowflake `IdentifierGenerator`；
  - `HealthIndicatorConfig.java`：聚合 MySQL、Redis、RabbitMQ、Nacos 的健康探测，确保 `/actuator/health/readiness` 真实可靠。

---

## 4. 环境信息与运维指南

| 项 | 说明 / 值 |
|---|---|
| 虚拟机 IP | `192.168.100.136`（Rocky Linux 8.9，root 用户） |
| 代码文件路径 | `/root/educloud/.worktrees/educloud-backend-foundation` |
| 一键启动脚本 | `bash deploy/scripts/start-dev.sh`（位于 VM 仓库根目录下，幂等执行） |
| 代码同步脚本 | 本地执行 `python scratch/sync_to_vm.py` 自动增量上传修改文件 |
| 预置账号（学生） | `fe_demo_10` / `FeDemo@2026`（Snowflake ID: `2091648316809035778`） |
| 预置账号（教师） | `demo_teacher@educloud.cn` / `EduCloud@2026`（ID: `9000000000000000001`） |
| 预置账号（管理） | `demo_admin` / `EduCloud@2026`（ID: `9000000000000000002`） |

---

## 5. 验证与验收记录

1. **后端全量编译与单元测试**：
   - 本地 `mvn clean test-compile` 7 个模块（`common`, `gateway`, `user`, `file`, `course`, `content`）**BUILD SUCCESS，0 错误**；
   - VM 虚拟机 `mvn clean package -DskipTests=true` 编译打包通过。
2. **前端构建验证**：
   - `student-portal`、`teacher-portal`、`admin-portal` 三端 `tsc && vite build` 均 **0 报错，顺利输出 dist**。
3. **MCP 浏览器自动化实测（Chrome DevTools）**：
   - 学生端（5173）：访问 `/learn/9000000000000000110`，验证 1280px 居中卡片布局、章节目录树展开、视频/讲义切换、点击“标记学完”进度条由 0% 实时跳变至 20%；
   - 教师端（5174）：访问 `/content`，验证章节目录结构与课件管理面板；
   - 管理端（5175）：访问 `/finance` 与 `/content-audit`，验证内容审核列表与表格状态徽标单行居中展示。

---

## 6. 近期 Git 提交记录（供追溯）

```text
7419250 docs: 整理并提交 M06 完结阶段交付与交接文档
9d530f6 fix(ui): 修复管理端及各端表格状态标签文字折行问题，添加 whitespace-nowrap 与宽度保护
9e693dc fix(student): 为课程学习进度上报添加并发防抖与互斥锁
97a1d2a feat(student): 课程学习页重构为经典居中双栏卡片模式并对接真实章节树
dff97a8 docs(spec): 课程学习页面居中重构设计规格说明
6e9e570 feat(content): 补全 IdentifierConfig 与 HealthIndicatorConfig，M06 全模块闭环
d5a6b57 fix(gateway): 将课程大纲与课件下载端点纳入 PUBLIC_READ 白名单
c71e0ca fix(deploy): 修复 start-dev.sh 中 content 微服务的 Nacos 凭据传递
```

---

## 7. 踩坑备忘录与避坑原则（⚠️ 接手必读）

1. **Snowflake ID 严禁 Number 化**：
   - 所有业务主键及外键（`id`, `courseId`, `chapterId`, `coursewareId` 等）均为 64 位雪花算法生成，在 Java 中为 `Long`，在前后端交互 JSON 及前端 TypeScript 中 **必须严格保持 `string` 类型**。严禁在前端使用 `Number(id)`，否则会导致 JavaScript 超过 53 位精度丢弃低位。
2. **API 信封结构规范**：
   - 所有后端接口均遵循统一信封 `Result<T>`：`{ "code": 200, "message": "...", "data": T, "traceId": "..." }`，前端已通过 `http.ts` 统一拦截和解包。
3. **数据库字符编码与连接参数**：
   - 新增表必须指定 `DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`，防止中文章节与课件名称出现乱码。
4. **服务启动与 VM 日志排查**：
   - 若服务启动未就绪，可直接在 VM 查看 `/tmp/educloud-live/{user,gateway,course,file,content}.log` 定位日志。

---

## 8. 下一步接力开发路线图：`M07 educloud-order`（订单中心）

后续接力工程师可直接启动 **M07** 模块的开发，核心范围与任务分解如下：

1. **模块定位**：`educloud-backend/educloud-order`（端口：业务 `8091` / 监控 `8092`）。
2. **核心业务与领域模型**：
   - **购物车管理**：添加/移除课程，多门课程合并下单；
   - **订单生成与幂等防重**：基于 Redis Token 机制防止重复点击下单，使用雪花算法生成全局唯一 `orderNo`；
   - **订单状态机**：`PENDING_PAYMENT`（待支付）→ `PAID`（已支付，触发选课事件）/ `CANCELLED`（超时关闭）/ `REFUNDED`（已退款）；
   - **超时自动关闭**：结合 RabbitMQ 延迟队列（TTL + 死信交换机），实现 15 分钟未支付订单自动取消并释放资源；
   - **跨服务事件发布**：订单支付成功后，向 RabbitMQ 发布 `Order.Paid` 领域事件，由 `educloud-course` 监听并自动为学员完成选课绑定。
3. **参考设计文档**：
   - 领域与服务划分：[`docs/superpowers/specs/2026-08-18-educloud-services-and-domains.md`](file:///d:/microservice/.worktrees/educloud-backend-foundation/docs/superpowers/specs/2026-08-18-educloud-services-and-domains.md)
   - 执行顺序与门禁：[`docs/superpowers/specs/2026-08-20-educloud-backend-module-execution.md`](file:///d:/microservice/.worktrees/educloud-backend-foundation/docs/superpowers/specs/2026-08-20-educloud-backend-module-execution.md)

---

*祝下一位接力工程师开发顺利！交接人：AI 助手（按用户要求编制），2026-08-24。*
